# Lessons Learned — Versimo

## Session 2026-04-13 — Session 44 (Implémentation lots/biens + tests quality gates)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| 44 | 2026-04-13 | problème | P0 | **bounding_box extraction non persisté en DB.** Le champ bounding_box était extrait par GPT mais pas stocké dans pro_rooms (pas de colonne). La page decoupe ne pouvait pas positionner les pièces sur le plan. | Ajout colonne JSONB bounding_box à pro_rooms (ALTER TABLE migration). Stockage dans le INSERT de extract/route.ts. | **Règle** : quand l'IA extrait des données structurées, vérifier que TOUS les champs sont persistés en DB — pas seulement retournés dans la réponse API. | règle-globale | lib/marchand/db.ts, app/api/pro/projects/[id]/extract/route.ts | fait | n/a |
| 44 | 2026-04-13 | problème | P0 | **Status `lots_defined` non accepté par validate/route.ts → 409 bloquant.** Le decoupe page set le status à lots_defined, mais validate ne l'acceptait pas dans validStatuses. Le flow decoupe→validation était silencieusement cassé. | Ajout "lots_defined" à validStatuses dans validate/route.ts. | **Règle** : quand on ajoute un nouveau status au ProjectStatusEnum, AUDITER toutes les routes qui font un status check (grep `validStatuses`, `project.status`) pour s'assurer que les transitions sont cohérentes. | règle-globale | app/api/pro/projects/[id]/validate/route.ts | fait | n/a |
| 44 | 2026-04-13 | problème | P1 | **Noms de champs API non vérifiés côté client.** La page decoupe utilisait `statusData.status` et `statusData.plan_image_url` alors que l'API retourne `project_status` et `project_plan_path`. Bug silencieux (champs undefined). | Fix des noms de champs + construction correcte de l'URL image plan. | **Règle** : avant de consommer une API côté client, TOUJOURS lire le code de la route serveur pour vérifier les noms de champs exacts de la réponse JSON. Ne jamais deviner. | règle-globale | app/projet/[id]/decoupe/page.tsx | fait | n/a |
| 44 | 2026-04-13 | problème | P1 | **PDF recommandations overflow : contenu dessiné sur l'ancienne page après pagination.** Dans dossier/pdf/route.ts, `recPage` était `const` → la nouvelle page créée pour l'overflow n'était pas utilisée pour le contenu suivant. | `const recPage` → `let recPage`, reassignment à la nouvelle page. | Quand une variable de page PDF est utilisée dans une boucle qui peut paginer, TOUJOURS la déclarer avec `let`. | agent-spécifique | app/api/pro/projects/[id]/dossier/pdf/route.ts | fait | n/a |
| 44 | 2026-04-13 | problème | P1 | **`[...map.entries()]` et `[...new Set()]` → TS2802.** Le spread sur iterables Map/Set nécessite `--downlevelIteration` ou target es2015+. | Remplacé par `Array.from()` systématiquement. | **Règle** : ne JAMAIS utiliser le spread `[...]` sur Map.entries(), Set, ou tout iterable non-Array. Toujours `Array.from()`. | règle-globale | CLAUDE.md | fait | n/a |
| 44 | 2026-04-13 | performance-ia | P1 | **Agents @qa et @fullstack timeout (>30 min chacun).** @qa a créé des tests pour les mauvaises fonctions. @fullstack a fait tous les changements structurels mais n'a pas eu le temps de créer la page principale. | Tests et page écrits manuellement par l'orchestrateur. | Les agents qui doivent (1) lire beaucoup de code contexte ET (2) produire un gros fichier timeout fréquemment. Mieux vaut faire les changements structurels soi-même si le contexte est déjà chargé. | agent-spécifique | — | fait | n/a |

## Session 2026-04-13 — Session 43 (Extraction rewrite + gates qualité + specs lots/biens)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| 43 | 2026-04-13 | problème | P0 | **GPT calcule les surfaces au lieu de lire celles écrites sur le plan.** Le prompt disait "calculate surface = length × width" en priorité. Les plans français ont les surfaces écrites en clair (ex: "25.8 m²"), mais GPT les ignorait et calculait à partir de dimensions mal lues → erreur 10x systématique (25.8m² → 241m²). | Prompt reécrit : STEP 3 priorité A = "FIRST: Look for surface values WRITTEN DIRECTLY on the plan". Le calcul L×W est le fallback, pas la priorité. | **Règle @ia** : quand l'IA doit extraire une donnée d'un document, toujours prioriser la LECTURE DIRECTE de la donnée sur le document avant tout calcul dérivé. | prompts | lib/marchand/plan-extractor.ts | fait | n/a |
| 43 | 2026-04-13 | problème | P0 | **Sanitization silencieuse = fondateur ne sait pas pourquoi une surface est vide.** sanitizeSurfaces corrigeait les erreurs (cap >80m², 10x) mais ne retournait aucun log. Le fondateur voyait des surfaces null sans explication. | sanitizeSurfaces retourne maintenant un `SanitizationEntry[]` log. validateExtraction transforme le log en warnings FR explicites par pièce. | **Règle** : toute correction automatique de données utilisateur DOIT produire un feedback visible ("X corrigé de A vers B parce que Z"). Jamais de correction silencieuse. | règle-globale | CLAUDE.md | fait | n/a |
| 43 | 2026-04-13 | problème | P1 | **Seuils quality gates fixes créent des faux positifs.** Cap 80m²/pièce et 300m²/étage sont trop bas pour maisons (loft 90m²), immeubles (plateau 200m²). | Seuils dynamiques par typeBien : 80/300 (appart), 150/500 (maison), 250/800 (immeuble). | **Règle @ia** : les seuils de validation doivent toujours être contextuels (type de bien, usage), jamais fixes. | prompts | lib/marchand/plan-extractor.ts | fait | n/a |
| 43 | 2026-04-13 | insistance | P0 | **Le fondateur a signalé 4 fois que les surfaces et bounding boxes ne matchent pas le plan.** Malgré 2 passes de corrections prompt, les résultats étaient toujours mauvais. Cause : les corrections n'étaient pas déployées + le prompt fondamental était mal structuré. | Prompt reécrit de zéro (6 étapes séquentielles), 8 gates post-extraction, sanitizeSurfaces avec détection 10x. | **Règle** : quand le fondateur signale un problème 2+ fois, vérifier que les corrections précédentes sont DEPLOYÉES avant d'en ajouter de nouvelles. | règle-globale | CLAUDE.md | fait | n/a |
| 43 | 2026-04-13 | préférence fondateur | P0 | **[PRÉFÉRENCE FONDATEUR] L'interface lots/biens DOIT être plan-centric.** "Il faut vraiment que l'interface soit simple, belle, efficace et géniale. On travaillera sur les plans, que ce soit avant, pendant ou après." Thomas veut VOIR le plan et cliquer dessus pour assigner les pièces aux lots — pas un tableau avec des dropdowns. | Specs écrites avec interface plan-centric (clic pièce → dropdown lot, couleurs par lot). | L'interface Versimo doit toujours être VISUELLE et centrée sur le plan/la photo, pas sur des formulaires texte. | founder-prefs | docs/founder-preferences.md | fait | propagé |
| 43 | 2026-04-13 | préférence fondateur | - | **[PRÉFÉRENCE FONDATEUR] L'étape lots/biens TOUJOURS visible, pas conditionnelle.** "On aura toujours un doute de savoir si pour un plan il y a un ou deux biens." Thomas ne veut pas que l'étape soit masquée par un type_bien. | Specs : étape toujours affichée, pré-remplie "1 bien = toutes les pièces" si cas simple. | Quand il y a un doute sur la structure d'un bien, montrer l'étape plutôt que la masquer. | founder-prefs | docs/founder-preferences.md | fait | propagé |
| 43 | 2026-04-13 | préférence fondateur | - | **[PRÉFÉRENCE FONDATEUR] Pricing marchand : ~9€/bien (pas par visuel, pas par immeuble).** Thomas raisonne en "dossiers à sortir". 1 bien = 1 dossier = 1 crédit. | Noté dans les specs lots/biens. Pricing à finaliser. | Le pricing doit être aligné sur l'unité de valeur du persona (le bien/dossier, pas la photo). | founder-prefs | docs/founder-preferences.md | fait | propagé |
| 43 | 2026-04-13 | pattern | - | **4 audits parallèles sur le même sujet = convergence forte.** Les 4 agents (QA, IA, Moi, Marchand) ont audité les mêmes gates. 5/7 corrections étaient demandées par 2+ agents. La convergence donne confiance dans les corrections. | — | Lancer 3-4 audits parallèles sur un sujet critique (gates, pipeline, UX clé) pour obtenir des corrections convergentes de haute confiance. | règle-globale | CLAUDE.md | fait | n/a |
| 43 | 2026-04-13 | performance-ia | P1 | **@product-manager timeout 2 fois sur les specs lots/biens (>100s).** Le prompt était trop long + lecture de 2 gros fichiers. L'orchestrateur a finalement écrit les specs lui-même. | Specs écrites directement par l'orchestrateur. | Les agents PM qui doivent lire des fichiers longs (>500 lignes) ET produire un document long (>100 lignes) timeout fréquemment. Découper en 2 appels ou écrire directement si le contexte est déjà en mémoire. | agent-spécifique | .claude/agents/product-manager.md | fait | n/a |

## Session 2026-04-13 — Session 42 (Plan editor + extraction PDF + convergence 10/10)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| 42 | 2026-04-13 | problème | P0 | **5 erreurs de build consécutives poussées en production.** Map spread (`[...prev, ...newMap]`), `as any` interdit par ESLint, accolade en trop, `files.del` au lieu de `files.delete`, variable unused. Cause : pas de `npx next build` avant push. | `npm install` + `npx next build` systématique avant push à partir du 5e fix. | **Règle permanente** : TOUJOURS lancer `npx next build` (pas juste lint) avant chaque `git push`. Si node_modules absent, `npm install` d'abord. Aucune excuse ("pas de node_modules") n'est acceptable. | règle-globale | CLAUDE.md | fait | propagé |
| 42 | 2026-04-13 | problème | P0 | **Object Storage SDK Replit retourne `value[0]` (tuple), pas `value`.** La route d'extraction faisait `Buffer.from(result.value as unknown as ArrayBuffer)` ce qui sérialisait le wrapper du tuple → PDF corrompu → InvalidPDFException. Le pattern correct existe dans `lib/db.ts` ligne 300 (`getImage()`). | Fix : `const buf = result.value[0]; planBuffer = Buffer.from(buf.buffer, buf.byteOffset, buf.byteLength)`. | **Règle** : copier le pattern `getImage()` de `lib/db.ts` pour tout nouveau download depuis Object Storage. Ne jamais caster `result.value` directement. | règle-globale | CLAUDE.md, app/api/pro/projects/[id]/extract/route.ts | fait | propagé |
| 42 | 2026-04-13 | problème | P0 | **serverComponentsExternalPackages obligatoire pour pdf-to-img sur Replit.** Sans cette config dans next.config.mjs, webpack bundle pdfjs-dist et ne copie pas le worker pdf.worker.mjs → InvalidPDFException en production. | Ajout `serverComponentsExternalPackages: ["pdf-to-img", "pdfjs-dist", "canvas"]` + commentaire DO NOT REMOVE. | **Règle** : tout package qui utilise un worker file (pdfjs-dist, sharp workers, etc.) doit être ajouté à `serverComponentsExternalPackages` dans next.config.mjs sur Replit. | règle-globale | next.config.mjs, CLAUDE.md | fait | propagé |
| 42 | 2026-04-13 | préférence fondateur | P0 | **[PRÉFÉRENCE FONDATEUR] "Le plan doit être l'élément CENTRAL, pas un toggle caché."** Demandé 6 fois. Le fondateur veut que le plan avec les zones colorées soit la première chose visible, en grand, pas derrière un bouton. | Plan editor affiché par défaut, pleine largeur (max-w-5xl), toggle supprimé. | **Règle** : quand un composant visuel est la VALEUR PRINCIPALE d'une page, il doit être l'élément hero. Jamais derrière un toggle, un accordion, ou un "voir plus". | founder-prefs | docs/founder-preferences.md | fait | propagé |
| 42 | 2026-04-13 | préférence fondateur | P0 | **[PRÉFÉRENCE FONDATEUR] "Ne me dis pas que c'est vérifié si tu ne l'as pas testé."** Le fondateur a reçu 5 erreurs de build consécutives après des messages "build vérifié". La simulation locale ne suffit pas — il faut le vrai build. | `npx next build` obligatoire, `npm install` si besoin. | **Règle** : ne JAMAIS confirmer qu'un build passe sans l'avoir exécuté. "J'ai simulé le flow" n'est pas une vérification. | founder-prefs | docs/founder-preferences.md | fait | propagé |
| 42 | 2026-04-13 | préférence fondateur | - | **[PRÉFÉRENCE FONDATEUR] "Ne recommence pas ce qui marche déjà."** Le fondateur a insisté pour réutiliser l'autocomplétion adresse et les dossiers existants au lieu de recréer. | Autocomplétion copiée de mes-biens, dossiers publics réutilisés. | **Règle** : avant de coder une feature, grep le codebase pour une implémentation existante. Le fondateur refuse qu'on recode ce qui fonctionne. | founder-prefs | docs/founder-preferences.md | fait | propagé |
| 42 | 2026-04-13 | pattern | P1 | **pdf-to-img > OpenAI Files API pour les PDF.** L'approche Files API (upload → file_id → reference) échouait silencieusement en production. La conversion PDF→PNG via pdf-to-img puis envoi comme image standard à GPT-4.1 est 100% fiable. | Migration complète vers pdf-to-img. Suppression de tout le code Files API/GPT-4o. | **Règle prompt** : pour analyser un PDF avec GPT vision, TOUJOURS convertir en image d'abord. Ne JAMAIS envoyer un PDF directement à l'API. | prompts | lib/marchand/plan-extractor.ts | fait | n/a |
| 42 | 2026-04-13 | pattern | P1 | **Bounding boxes en % > coordonnées pixels.** GPT-4.1 vision ne connaît pas la résolution de l'image mais estime bien les positions relatives (0-100%). Les pièces sont positionnées à leur emplacement réel sur le plan. | Ajout instruction #9 dans le system prompt + champ bounding_box dans le schema. | **Règle prompt** : pour toute extraction spatiale, demander des pourcentages (0-100) plutôt que des pixels absolus. | prompts | lib/marchand/plan-extractor.ts | fait | n/a |

## Session 2026-04-10 — Session 41 (Parcours marchand implémentation + audits)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| 41 | 2026-04-10 | pattern | - | **Triage bugs d'audit = étape obligatoire.** 80% des bugs P0/P1 identifiés par les 5 rapports d'audit étaient DÉJÀ fixés dans les commits précédents de la même session. Sans triage rigoureux, on aurait re-fixé des bugs déjà résolus, gaspillant du temps et risquant des régressions. | Triage systématique avant implémentation | **RÈGLE : après réception d'un rapport d'audit, TOUJOURS cross-référencer chaque bug avec le code actuel (Read + Grep) avant de coder un fix. Un bug documenté n'est pas forcément un bug actif.** | règle-globale | CLAUDE.md | fait | n/a |
| 41 | 2026-04-10 | problème | P1 | **Agents d'audit timeout sur les parcours longs.** 3/14 agents lancés en background ont timeout (QA parcours complet, reviewer cross-audit, audits étapes 5-7). Les parcours 7 étapes sont trop longs pour un seul agent. | Découpage en batches de 2-3 étapes | **RÈGLE : pour les audits step-by-step, max 3 étapes par agent. Découper le parcours en batches. Un agent qui couvre 7 étapes + lecture de code source timeout à 100%.** | agent-spécifique | CLAUDE.md | fait | n/a |
| 41 | 2026-04-10 | pattern | - | **Optimistic UI + revert on failure** est plus robuste que fire-and-forget pour les mutations serveur. Les recommandations accept/reject silencieusement swallowed les erreurs avant le fix. | Implémenté sur recommendations page | **RÈGLE @fullstack : toute mutation côté serveur (PATCH, PUT, DELETE) avec feedback UI doit utiliser le pattern optimistic + revert, JAMAIS fire-and-forget.** | agent-spécifique | CLAUDE.md | fait | n/a |
| 41 | 2026-04-10 | problème | P1 | **LEFT JOIN obligatoire pour les requêtes rooms.** La route generate utilisait un INNER JOIN qui excluait les pièces sans lot_id — bug silencieux car aucune pièce sans lot dans les données de test. | LEFT JOIN + NULLS LAST dans generate route | **RÈGLE @fullstack : les requêtes SQL qui joignent pro_rooms et pro_lots doivent TOUJOURS utiliser LEFT JOIN (une pièce peut exister sans lot assigné).** | aucune | app/api/pro/projects/[id]/generate/route.ts | fait | n/a |
| 41 | 2026-04-10 | problème | P1 | **Auto-trigger génération sur page reload.** La page génération lançait un POST /generate à chaque mount du useEffect, même si la génération était déjà terminée. Un simple refresh relançait tout. | Check status API avant de trigger | **RÈGLE @fullstack : les pages avec side-effects automatiques (génération, extraction) doivent TOUJOURS vérifier l'état actuel avant de déclencher l'action. Pattern : check status → si done/en cours → afficher résultat → sinon trigger.** | aucune | app/projet/[id]/generation/page.tsx | fait | n/a |
| 41 | 2026-04-10 | préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : isDirty doit TOUJOURS être un useState (pas useRef) car il drive le comportement beforeunload qui dépend du re-render. | useState utilisé dans validation page | Le fondateur vérifie que les guards de navigation fonctionnent. isDirty = useState obligatoirement. | aucune | - | fait | n/a |

## Session 2026-03-28 (Audit complet site + parcours d'achat)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Statut |
|---|---|---|---|---|---|---|---|
| 28 | 2026-03-28 | problème | P0 | **Parcours d'achat jamais testé end-to-end.** `lib/stripe.ts` avait les anciens prix (4,90€/14,90€), le Pro était en `mode: "payment"` au lieu de `subscription`, les 4 packs de recharge n'existaient pas (erreur "Pack invalide"), zéro feedback post-achat, message "Crédits insuffisants" sans lien. Score Thomas 5.3/10. | Sync pricing v3, mode subscription Pro, 4 recharges ajoutées, bandeau post-achat, message épuisement actionnable | **RÈGLE ABSOLUE @qa/@fullstack : tester le parcours d'achat complet (clic CTA → auth → rétractation → checkout Stripe → retour) pour CHAQUE persona AVANT tout déploiement. Vérifier que `lib/stripe.ts` PACKS correspond exactement à l'UI pricing.** | appliqué |
| 28 | 2026-03-28 | problème | P0 | **Backend et frontend désynchronisés sur le pricing.** L'UI affichait pricing v3 (9,90€/29€/mois) mais le backend avait les anciens prix. Aucun test ne vérifiait la cohérence. | Sync `lib/stripe.ts` avec `docs/product/pricing-strategy.md` v3 | **RÈGLE @fullstack : quand le pricing change, mettre à jour dans CET ORDRE : (1) pricing-strategy.md, (2) lib/stripe.ts, (3) app/pricing/page.tsx, (4) app/page.tsx section pricing, (5) landing pages. Grep `priceCents\|credits:` pour vérifier la cohérence.** | appliqué |
| 28 | 2026-03-28 | problème | P1 | **Vocabulaire "crédit" résiduel dans le backend.** Les messages d'erreur API disaient "Crédits insuffisants" alors que l'UI utilisait "visuels". Le compteur AuthButton affichait "X cr." au lieu de "X visuels". | Tous les messages API + AuthButton corrigés | **RÈGLE @fullstack : quand le vocabulaire change (crédit→visuel), grep `crédit\|credit\|cr\.` dans TOUS les fichiers (app/, components/, lib/) — pas seulement les pages visibles.** | appliqué |
| 28 | 2026-03-28 | problème | P1 | **Page annonce : room_type affiché en anglais brut** ("bedroom_children" au lieu de "Chambre enfant"). Le dictionnaire `ROOM_TYPE_LABELS` dans `lib/constants.ts` était incomplet. | Ajout bedroom_adults, bedroom_children dans ROOM_TYPE_LABELS + ROOM_ORDER | **RÈGLE @fullstack : quand on ajoute un room_type dans `lib/room-types.ts`, TOUJOURS ajouter le label FR dans `lib/constants.ts` ROOM_TYPE_LABELS en même temps.** | appliqué |
| 28 | 2026-03-28 | problème | P1 | **ExportPortail inline superposé au contenu** — boutons qui chevauchent l'annonce. | Nouveau composant ExportPortailModal.tsx (modal avec backdrop, Escape, scroll lock) | Les composants d'action complexes (export, génération batch) doivent être dans des modals, pas inline dans la page. | appliqué |
| 28 | 2026-03-28 | insistance | P0 | **Build non testé par les agents — 2 erreurs consécutives.** Apostrophe JSX non échappée (l.1173 mes-biens/[id]) + useEffect missing dep (mes-dossiers) + setInterval crash admin-auth. Le fondateur a dû signaler les erreurs. | Fixes appliqués, mais le process reste fragile | **RENFORCEMENT RÈGLE P0 session 27c : les agents @fullstack DOIVENT exécuter `npx tsc --noEmit` AVANT de commiter. Si timeout empêche l'exécution, le signaler explicitement au fondateur — ne JAMAIS commiter sans vérification.** | appliqué |
| 28 | 2026-03-28 | pattern | - | **Audit complet 20 pages + 31 composants en 6 rapports parallèles** = couverture exhaustive. Découpage en batches de 4-5 fichiers max pour éviter les timeouts. | — | Anti-timeout pour audits : max 5 fichiers par agent. Découper systématiquement. Les agents à 14 fichiers timeout à 100%. | appliqué |
| 28 | 2026-03-28 | pattern | - | **Corrections P0→P1→P2 en cascade** avec audits persona entre chaque round = progression mesurable (Thomas 5.3→~9.0, Claire 7.2→~9.5, Léa 8.2→~9.5). | — | Pattern efficace : fix backend critique → fix UX → audit persona → corrections finales. | appliqué |
| 28 | 2026-03-28 | préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : les espaces entre sections étaient "trop grands". py-16 sm:py-24 réduit à py-10 sm:py-14 (~35% de réduction). | Spacing réduit sur 6 pages | Calibration fondateur : style compact Apple, pas style blog aéré. Les valeurs py-10 sm:py-14 sont la nouvelle baseline. | appliqué |
| 28 | 2026-03-28 | préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : "crédit" n'est pas assez clair. 1 crédit = 1 quoi ? Validé "visuel" comme terme unique commercial. | Remplacement global crédit/génération → visuel (12 fichiers, 70 occurrences) | **RÈGLE PERMANENTE : le terme commercial est "visuel" (pas "crédit", pas "génération"). "crédit" réservé aux CGV. "génération" réservé aux états machine (loading/erreur).** | appliqué |

| 28 | 2026-03-28 | problème | P0 | **Migration gpt-image-1→1.5 sans paramètre `action: "edit"`** — le modèle basculait en mode "generate" au lieu d'"edit", recréant la scène de zéro. Cause racine de la perte de géométrie signalée par le fondateur. | Ajout `action: "edit"` sur les 2 blocs image_generation (v27) | **RÈGLE @ia : tout changement de modèle IA nécessite : (1) lecture de la doc API du nouveau modèle, (2) identification des nouveaux paramètres obligatoires, (3) test sur 3+ photos avant déploiement, (4) bump PROMPT_VERSION.** | appliqué |
| 28 | 2026-03-28 | problème | P1 | **Dimensions mobilier en cm sans référence spatiale** — le modèle ne peut pas calibrer "230cm sofa" sans savoir la taille de la pièce. Les SDB (facteur 6x de variation) étaient les plus impactées (1 seule dimension sur 8 éléments). | Ancrage architectural (porte 204cm, plafond 250cm, carreaux), scaling DOWN conditionnel, dimensions SDB explicites (v28-v29) | **RÈGLE @ia : chaque roomFurnitureOverride doit avoir des dimensions cm pour CHAQUE élément + des références d'échelle architecturales + du scaling conditionnel (si compact → réduire).** | appliqué |
| 28 | 2026-03-28 | problème | P1 | **Corrections de prompts non propagées aux builders Flux** — les corrections v28 (échelle, WALL_PRESERVATION) n'étaient appliquées qu'aux builders OpenAI Responses, pas aux builders Flux. @ia a identifié 6 trous sur les builders Flux passe 1 et 8 sur passe 2. | Propagation complète v30 — tous builders Responses + Flux ont les mêmes directives | **RÈGLE @ia : toute modification de prompt DOIT être appliquée dans les 4 fonctions builder (surfacesResponses, surfacesFlux, furnitureResponses, furnitureFlux) + vérification par @ia après.** | appliqué |
| 28 | 2026-03-28 | problème | P1 | **Admin page : violation Rules of Hooks** — useEffect déclaré après des return conditionnels → crash React potentiel. | useEffect déplacé avant les returns conditionnels | **RÈGLE @fullstack : tous les hooks (useState, useEffect, useCallback, useMemo, useRef) DOIVENT être AVANT tout return conditionnel dans un composant React.** | appliqué |
| 28 | 2026-03-28 | insistance | P0 | **Le fondateur a signalé 3 erreurs de build consécutives** (apostrophe JSX, useEffect deps, setInterval). Les agents ne testent toujours pas le build malgré la règle P0 de la session 27c. | Fixes appliqués au cas par cas | L'environnement CI n'a pas Sentry/@types/node installés, ce qui empêche le build complet. Solution : tester au minimum `npx tsc --noEmit` en filtrant les erreurs pré-existantes. Si impossible, le SIGNALER au fondateur. | ouvert |
| 28 | 2026-03-28 | préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : le fondateur ne veut PAS tester le build lui-même. "Je ne suis pas là pour tester" — frustration exprimée 2 fois dans la session. Les agents doivent être autonomes sur la QA de build. | — | Les agents doivent intégrer le build check comme étape NON NÉGOCIABLE de leur workflow. Si le build check est impossible dans l'environnement, documenter la limitation et proposer une alternative. | ouvert |
| 28 | 2026-03-28 | préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : quand on clique "Acheter", ça doit lancer l'achat DIRECTEMENT — pas ouvrir une page intermédiaire. La checkbox de rétractation est une friction inutile puisque Stripe gère les conditions. | Checkout direct + suppression checkbox rétractation | Les parcours d'achat doivent être le plus court possible : clic → auth si nécessaire → Stripe. Zéro étape intermédiaire. |appliqué |
| 28 | 2026-03-28 | préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : les boutons dossier/annonce doivent refléter l'état réel ("Voir" si existe, "Générer" si n'existe pas, "Regénérer" si existe déjà). Pas de lien vers une liste globale depuis une fiche de bien. | Boutons contextuels + liens directs sur fiche bien | Les actions sur une entité doivent être contextuelles à cette entité — jamais de redirection vers une liste globale. | appliqué |
| 28 | 2026-03-28 | préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : le backoffice admin doit être "propre comme le reste du site" — même header, footer, design system. Pas de styles inline. | Refonte admin Tailwind + header/footer Versimo + tab Utilisateurs | Le backoffice n'est pas un "outil interne moche" — c'est une extension du produit. Même standard de qualité. | appliqué |
| 28 | 2026-03-28 | recommandation | P1 | **Les agents admin timeout systématiquement** sur les gros fichiers (admin/page.tsx ~700 lignes). 3 timeouts consécutifs avant de réussir. | Instructions anti-timeout : "commite après chaque section" | Les fichiers de plus de 500 lignes doivent être traités par SECTIONS avec commit intermédiaire. L'agent doit commiter dès qu'une section est terminée, pas attendre la fin. | appliqué |

## Session 2026-03-27c (Landing pages personas + refonte pricing)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Statut |
|---|---|---|---|---|---|---|---|
| 27c | 2026-03-27 | problème | P0 | Apostrophes `'` non échappées dans le JSX reviennent à CHAQUE session (15 occurrences sur 5 fichiers cette session) | Passe globale `'` → `&apos;` sur 5 fichiers | Règle @fullstack : dans le JSX rendu (entre balises HTML), TOUJOURS utiliser `&apos;` pour les apostrophes. Les strings JS (`const`, objets) gardent `'`. Vérifier AVANT commit. | ouvert |
| 27c | 2026-03-27 | insistance | P0 | 6 erreurs de build consécutives dans la même session (apostrophes JSX ×2, TypeScript gpt-image-1.5 ×3, pixelmatch null). Le fondateur ne doit JAMAIS être celui qui teste le build — c'est le travail des agents. | `npx tsc --noEmit` + `npx next lint` exécutés AVANT commit | **RÈGLE ABSOLUE @fullstack/@qa : exécuter `npx tsc --noEmit` ET `npx next lint` AVANT chaque commit sur des fichiers .ts/.tsx. Si l'un des deux échoue, ne PAS commiter. Corriger d'abord. Le fondateur ne doit jamais voir une erreur de build.** | ouvert |
| 27c | 2026-03-27 | problème | P1 | Galeries multi-styles avec images placeholder identiques (3x même image) détruisent la crédibilité — pire qu'aucune galerie | Galeries commentées en attendant vraies images | Ne JAMAIS afficher une galerie avec des images identiques labellées comme différents styles. Commenter la section jusqu'à avoir le contenu réel. | appliqué |
| 27c | 2026-03-27 | problème | P1 | Témoignages fictifs utilisant les noms de persona (Thomas B., Claire D., Léa M.) détectés par les audits comme "faux" | Remplacés par témoignages anonymisés ou chiffres factuels | Ne JAMAIS utiliser les noms des personas comme témoignages. Anonymiser (métier + ville) ou utiliser des chiffres factuels. | appliqué |
| 27c | 2026-03-27 | pattern | - | Itération audit→fix→re-audit avec persona agents (Thomas/Claire/Léa) en parallèle = progression rapide (5.5→9.05 en 5 rounds) | - | Pattern efficace : 3 audits persona en parallèle, corrections consolidées, re-audit. Max 5 rounds. | appliqué |
| 27c | 2026-03-27 | préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : 3 tiers pricing max (Découverte gratuit / Starter one-shot / Pro abonnement). Seul le Pro est en abonnement. Recharge au prix du tier. | pricing-strategy.md v3 | Modèle hybride : one-shot pour usage ponctuel (Léa), abonnement pour usage récurrent (Thomas). | appliqué |
| 27c | 2026-03-27 | préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : ne JAMAIS dire "MVP" — c'est un vrai projet, on vise la perfection. Le fondateur a recadré 2 fois dans la session quand l'orchestrateur proposait de s'arrêter ou de considérer un score "suffisant". | - | Le seuil est 9.5/10 minimum (préférence documentée session 26c). Ne JAMAIS proposer d'arrêter les itérations avant d'atteindre le seuil. | ouvert |
| 27c | 2026-03-27 | préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : ne JAMAIS être paresseux. Le fondateur a dit "ne soit plus paresseux dans cette session" après que l'orchestrateur a proposé de s'arrêter à 8.4-8.5. L'exigence est constante, pas conditionnelle. | - | Calibrer les agents sur l'exigence maximale dès le round 1. Ne pas attendre que le fondateur recadre. | ouvert |
| 27c | 2026-03-27 | préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : l'encart Mode Pro sur la homepage était "trop gros". Le fondateur veut du compact, du discret. Les encarts doivent pointer vers les pages dédiées, pas reproduire tout le contenu. | Encart remplacé par 3 mini-cartes personas | Principe : les sections homepage doivent être des teasers compacts qui redirigent, pas des blocs d'information autonomes. | appliqué |
| 27c | 2026-03-27 | insistance | P0 | Le fondateur a dû tester le build 6 fois parce que les agents ne le faisaient pas. "6e erreur de suite pas acceptable, je ne suis pas là pour tester" — frustration maximale. | Règle P0 tsc+lint avant commit | Les agents @fullstack doivent être programmés pour exécuter le build check comme DERNIÈRE étape systématique. Ajouter dans le prompt agent. | ouvert |
| 27c | 2026-03-27 | problème | P1 | Agents @creative-strategy et @product-manager timeout sur les tâches pricing (2 timeout simultanés). L'orchestrateur a pris le relais et écrit les livrables lui-même. | Livrables écrits directement | Pour les tâches critiques avec risque timeout : écrire directement au lieu de déléguer. Les agents sont utiles pour les audits, pas toujours pour la production de contenu long. | appliqué |

## Session 2026-03-27b (Audit IA + benchmark + migration GPT Image 1.5)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Statut |
|---|---|---|---|---|---|---|---|
| 27b | 2026-03-27 | problème | P1 | Audits visuels avec images Unsplash MEUBLÉES invalident le test de préservation géométrie — le modèle régénère au lieu d'éditer | Audit géométrie refait sur vraies photos de chantier (#37-44) → 8.5-9.5/10 | TOUJOURS utiliser des pièces VIDES pour tester la géométrie. Les images meublées ne testent que le style, pas la préservation. | appliqué |
| 27b | 2026-03-27 | problème | P1 | Agents d'audit visuel timeout systématique (3+ images = >25 min, images PNG 2-3 MB) | Batch réduit à 3 images. Yann batch 1 OK (5 min), batch 2 timeout, Lucas timeout | Règle anti-timeout : max 3 images par agent d'audit visuel. Découper en batches. Si timeout, relancer (ne pas faire manuellement). | ouvert |
| 27b | 2026-03-27 | problème | P0 | Agents textuels lourds (7 styles à auditer) timeout systématiquement (~28-35 min) | Abandonnés, remplacés par audits visuels sur images réelles | Règle : ne JAMAIS donner plus de 3-4 styles à auditer à un agent en une passe. Découper en batches de 3. | appliqué |
| 27b | 2026-03-27 | pattern | - | Benchmark baseline v24 mesuré sur prod : latence moyenne 131s (107-161s) — données réelles, zéro invention | Rapport JSON commité dans benchmarks/history/ | Toujours mesurer la baseline AVANT de changer quoi que ce soit. Les données mesurées sont la seule source de vérité. | appliqué |
| 27b | 2026-03-27 | recommandation | P0 | Latence 131s vs Gepetto 15s = 9x plus lent. Défaut fatal pour la rétention (@elon) | Migration GPT Image 1.5 codée (v26), non déployée | Migrer vers GPT Image 1.5 dès le prochain déploiement. Objectif : <40s. | ouvert |
| 27b | 2026-03-27 | recommandation | P1 | Zéro détection automatique de régression — process 100% humain | Benchmark + scripts run.ts/compare.ts + seuils d'alerte implémentés | Cron hebdo sur 3 images benchmark. Exécuter benchmark avant chaque bump PROMPT_VERSION. | ouvert |
| 27b | 2026-03-27 | préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : la préservation de la pièce d'origine (taille, angle, géométrie) est le critère n°1 absolu | Grille pondérée : géométrie ×3, éléments structurels ×3 (poids max) | Protocole prompts : G1 et G2 sont BLOQUANTS. Score <7 sur géométrie = NO-GO. | appliqué |

## Session 2026-03-27 (Sprint 22 + V2a + PDF portrait + agents)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Statut |
|---|---|---|---|---|---|---|---|
| 27 | 2026-03-27 | problème | P0 | Flux Depth Pro détruit la géométrie en passe 2 (hallucination fenêtre #41, perte voûte #42, Yann 4.2/Lucas 5.0) | Flux désactivé en passe 2. Si GPT-4.1 échoue P2, retourner P1 seule. | Règle CLAUDE.md : NE JAMAIS utiliser Flux en passe 2. Flux autorisé P1 uniquement. | appliqué |
| 27 | 2026-03-27 | problème | P0 | Itérations détruisent le mobilier existant (#33 tout Japandi perdu) — l'image source était la passe 1 vide au lieu de l'output meublé | Directives préservation dans iteration-prompt.ts + vérification image source = output | Règle CLAUDE.md : itérations TOUJOURS sur l'image output, JAMAIS sur passe 1 | appliqué |
| 27 | 2026-03-27 | problème | P0 | "Do NOT attach to walls" sabote les demandes légitimes d'étagères murales (#33) | allowWallMounted flag dans custom-prompt.ts + iteration-prompt.ts | Règle CLAUDE.md : autoriser wall-mounted SI l'utilisateur le demande explicitement | appliqué |
| 27 | 2026-03-27 | problème | P1 | PAPPERS_API_KEY="..." (placeholder) évaluée truthy → appel Pappers timeout 8s avant fallback gouv.fr | hasPappersKey() rejette placeholders, skip direct vers gouv.fr | Règle @fullstack : toujours valider les clés API contre les placeholders, pas juste truthy | appliqué |
| 27 | 2026-03-27 | problème | P1 | Police custom /compte = promesse non tenue — 5 polices proposées mais ignorées partout sauf aperçu | Mapping serif→TimesRoman, sans→Helvetica dans PDF. fontFamily sur MerchantBrandWrapper web | pdf-lib ne supporte pas embed .ttf sans chantier significatif — documenter la limitation | appliqué |
| 27 | 2026-03-27 | pattern | - | Audits croisés Yann+Lucas+Camille en parallèle (6 gen par batch) = résultats en 2 min au lieu de timeout | - | Anti-timeout : toujours découper les audits visuels en batch de 6 max. 28 images = 4 agents parallèles. | appliqué |
| 27 | 2026-03-27 | pattern | - | Mood sentences au début des furniturePrompts améliorent la composition — validé par Yann + Lucas | - | Toujours ouvrir un prompt créatif par une phrase d'INTENTION avant la liste technique de meubles | appliqué |
| 27 | 2026-03-27 | pattern | - | Pipeline copy→design→UX→Thomas→QA sur V2a = couverture complète, 9.1→9.5 Thomas en 2 itérations | - | Pattern à capitaliser : audit 3 experts → corrections → audit persona → QA | appliqué |
| 27 | 2026-03-27 | recommandation | P1 | Agent-factory bloqué par permissions Write sur .claude/agents/ — timeout 2x | Agents créés manuellement par l'orchestrateur | Agent-factory a besoin de permissions Write auto-approved sur .claude/agents/ | ouvert |
| 27 | 2026-03-27 | préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : le PDF doit être au niveau des meilleurs PDFs possibles, type fiche de bien immobilier | PDF refait en portrait A4, photos empilées, page marchand, QR code | Les livrables envoyés aux acquéreurs sont le critère n°1 — toujours viser le niveau Sotheby's/Barnes | appliqué |

## Session 2026-03-26c (marathon)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Statut |
|---|---|---|---|---|---|---|---|
| 26c | 2026-03-26 | 1. Problème | P0 | Replit autoscale tue le worker après envoi de la réponse HTTP — tout async fire-and-forget est perdu (saveUserPhoto, logGeneration) | saveUserPhoto + logGeneration await AVANT NextResponse.json() | Règle @fullstack : sur Replit autoscale, JAMAIS de fire-and-forget après la réponse. Tout save critique doit être await avant return. | appliqué |
| 26c | 2026-03-26 | 1. Problème | P0 | Migration user_id cross-provider : meme email avec id different (credentials→Google) crée un 2e user, photos orphelines | Transaction complète dans ensureUser() : migre photos+properties+dossiers+purchases+merchant, DELETE old row | Règle @fullstack : toute table avec user_id DOIT être dans le bloc de migration de ensureUser(). Checklist à maintenir. | appliqué |
| 26c | 2026-03-26 | 1. Problème | P1 | PDF crash sur caractères Unicode non-WinAnsi (espace insécable 0x202F) | sanitizeForPdf() + safeDrawText() wrapper sur tous les drawText | Règle @fullstack : pdf-lib avec StandardFonts = WinAnsi only. Toujours sanitizer les strings avant drawText. | appliqué |
| 26c | 2026-03-26 | 2. Insistance | P0 | Fondateur a demandé 5+ fois de fixer les images hero homepage — elles disparaissaient à chaque deploy (Object Storage instable) | Images commitées en statique dans public/ (/imageavant.jpg, /imageapres.jpg) | Règle : les assets critiques de la homepage DOIVENT être dans le repo git, pas dans Object Storage. Zéro dépendance runtime pour le hero. | appliqué |
| 26c | 2026-03-26 | 2. Insistance | P1 | Fondateur a demandé 4+ fois de fixer le modal auth sur mobile — le fix ne marchait pas car items-center ne fonctionne pas sur iOS Safari avec overflow | Pattern bottom sheet (items-end mobile, items-center desktop, 100dvh, safe-area) | Règle @design : les modals sur mobile DOIVENT utiliser le pattern bottom sheet. items-center + overflow-y-auto est cassé sur iOS Safari. | appliqué |
| 26c | 2026-03-26 | 2. Insistance | P1 | Fondateur a demandé 3+ fois les accents — chaque nouvel agent/composant introduisait des accents manquants | Passe globale ~100 accents + eslint no-img-element global | Règle : les agents @fullstack DOIVENT vérifier les accents français dans les strings UI avant de livrer. Ajouter dans le protocole base agent. | appliqué |
| 26c | 2026-03-26 | 5. Pattern efficace | - | Audits Thomas+Marc en parallèle avec corrections @fullstack = cycle d'amélioration rapide (5.6→8.8 Thomas, 5.6→9.4 Marc en 3 passes) | - | Pattern à capitaliser : audit persona → corrections → re-audit. Max 3 passes. Les 2 personas couvrent producteur (Thomas) et consommateur (Marc). | appliqué |
| 26c | 2026-03-26 | 5. Pattern efficace | - | Benchmark @copywriter avant implémentation (SeLoger/Sotheby's) = prompts beaucoup plus riches dès la 1ère passe | - | Toujours benchmarker le secteur avant de rédiger un prompt LLM. Le benchmark docs/copy/annonce-immobiliere-benchmark.md est réutilisable. | appliqué |
| 26c | 2026-03-26 | 8. Préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : Thomas veut 10/10 minimum sur les livrables marchand — 8/10 n'est pas acceptable pour un produit qu'il montre à ses acquéreurs | - | Calibrer les audits Thomas/Marc avec un seuil de 9.5/10 minimum. Relancer les agents jusqu'à atteindre ce seuil. | ouvert |
| 26c | 2026-03-26 | 8. Préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : Thomas veut voir les photos de sa galerie immédiatement après génération, sans exception — c'est LE critère d'acceptation n°1 | - | La persistance des photos est plus importante que la vitesse de réponse. 2-3s de latence supplémentaire = acceptable. Photo perdue = inacceptable. | appliqué |
| 26c | 2026-03-26 | 8. Préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : Thomas préfère les titres au format portail immo standard (T3 60 m²) plutôt que des titres créatifs — crédibilité > originalité | - | Les formats standard du secteur sont toujours préférés aux innovations créatives pour les livrables B2B. | appliqué |
| 26c | 2026-03-26 | 8. Préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : Thomas teste en production sur mobile — chaque bug visible = perte de confiance. Les screenshots sont le critère de vérité, pas le code. | - | Les agents QA doivent simuler le parcours mobile, pas juste lire le code. Prioriser les tests visuels sur les tests logiques. | ouvert |
| 26c | 2026-03-26 | 7. Performance IA | P2 | @creative-strategy timeout 2x (plan blog + titres) — agent trop lent sur les tâches de recherche web | - | Réduire le scope des tâches creative-strategy à des questions précises. Pas de "fais tout en une passe". | ouvert |
| 26c | 2026-03-26 | 7. Performance IA | P2 | @qa timeout 1x sur audit exhaustif 10 pages — scope trop large pour un seul agent | Découpage en 2 agents parallèles (pages 1-5 + pages 6-10) | Règle : ne jamais donner plus de 5 pages à auditer à un seul agent QA. Découper systématiquement. | appliqué |

## Session 2026-03-31 (Rename, accents, multi-photo fix, font-light, latency, product changes v2)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Statut |
|---|---|---|---|---|---|---|---|
| 31 | 2026-03-31 | préférence fondateur | P0 | **[PRÉFÉRENCE FONDATEUR] : Ne JAMAIS demander confirmation pour corriger des bugs QA — toujours corriger immédiatement. On vise la perfection, pas le "suffisant".** Thomas considère que demander "tu veux que je corrige ?" quand des bugs sont identifiés est une perte de temps. Les bugs trouvés par QA doivent être corrigés dans la foulée, sans question. | Appliqué dans cette session | **RÈGLE ABSOLUE : quand @qa identifie des bugs, les corriger immédiatement sans demander confirmation au fondateur. La perfection est le standard, pas l'option.** | appliqué |
| 31 | 2026-03-31 | problème | P1 | Accents français manquants sur 15+ fichiers — "Reessayer", "telecharger", "decoration", "generees", etc. Certains étaient des unicode escapes (\u00E9) au lieu de vrais caractères UTF-8. | Grep exhaustif + corrections manuelles + script Python pour unicode escapes | **RÈGLE @fullstack : après chaque modification de texte FR, grep `[a-z]ee[a-z]\|etat\|genere\b\|interieur\|exterieur` pour détecter les accents manquants. Ne jamais utiliser \u00XX dans les strings JS.** | appliqué |

## Session 2026-04-02 (v37/v38 prompt corrections + UX fixes + iteration audit)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| 30 | 2026-04-02 | problème | P0 | **Itération régénère la scène au lieu d'éditer.** Audit #98 : Yann 5.9/10, Lucas 4.5/10. Le modèle supprime four, crédence, accessoires quand on demande "enlever quelque chose au sol". Cause racine : prompt adjust dit "keep everything" abstraitement sans inventaire concret du mobilier existant. | SURGICAL EDIT framing + inventaire mental + anti-régénération dans iteration-prompt.ts (v38) | **RÈGLE CLAUDE.md : les prompts d'itération DOIVENT contenir (1) un framing SURGICAL EDIT en première position, (2) une directive d'inventaire mental, (3) une liste explicite de ce qui ne doit PAS changer pour les pièces spécialisées (kitchen appliances, bathroom fixtures).** | prompts | CLAUDE.md (règles prompts IA), lib/iteration-prompt.ts | fait | propagé |
| 30 | 2026-04-02 | problème | P1 | **Cellar builder manquait les corrections v37** (anti-fenêtre + anti-warm shift). Détecté par @qa après implémentation initiale, pas par le développeur. | Fix appliqué dans route.ts + generation-pipeline.ts (commit 517636c) | **RÈGLE : après chaque modification de prompt, @qa DOIT vérifier EXHAUSTIVEMENT tous les builders (y compris cellar, laundry, wc) — pas juste les builders principaux.** | agent-spécifique | .claude/agents/qa.md | fait | n/a |
| 30 | 2026-04-02 | problème | P1 | **Camera LOCKED absent des 2 builders restyle** (indoor + outdoor) alors que les adjust l'avaient. Inconsistance détectée par @qa. | Propagé dans les 2 builders restyle de iteration-prompt.ts | **RÈGLE : toute directive ajoutée dans les builders adjust DOIT être vérifiée pour propagation dans les builders restyle et vice-versa. Les 4 builders d'itération doivent rester alignés.** | prompts | lib/iteration-prompt.ts | fait | propagé |
| 30 | 2026-04-02 | problème | P1 | **window.confirm() natif utilisé à 4 endroits** — rupture UX vs les modals déjà en place (RefineModal, AuthModal). Le fondateur a signalé les "native confirm dialogs" comme problème. | ArchiveConfirmModal créé (3 endroits) + InlineGenerator modal inline (1 endroit) | **RÈGLE @fullstack : ne JAMAIS utiliser window.confirm() ou window.alert(). Toujours utiliser un composant modal custom avec le design system Versimo.** | règle-globale | CLAUDE.md | fait | n/a |
| 30 | 2026-04-02 | problème | P2 | **scrollToElement delay 150ms insuffisant** pour les animations CSS 700ms + async image loading. Le scroll arrivait avant que le DOM ne soit stable. | Delay augmenté à 600ms | **RÈGLE @fullstack : scrollToElement delay ≥ durée max des animations CSS du projet (actuellement 700ms). 600ms = valeur de base.** | aucune | — | fait | n/a |
| 30 | 2026-04-02 | pattern | - | **@qa vérifie APRÈS @ia valide** = workflow efficace. @ia confirme la direction (GO/NO-GO), @qa vérifie l'exhaustivité (PASS/FAIL par builder). 2 anomalies trouvées par @qa que @ia n'avait pas vues (cellar + Camera LOCKED). | — | Pattern @ia→@qa à systématiser pour tout changement de prompt : validation conceptuelle puis vérification exhaustive. | aucune | — | fait | n/a |
| 30 | 2026-04-02 | pattern | - | **"95%+ identical pixels" comme framing** est un compromis entre "pixel-identical" (trop strict, leçon Sprint 17) et "visually identical" (trop laxiste). @ia valide avec réserve : surveiller 5 prochaines itérations. | — | Si le modèle devient inactif (output = input), reformuler en "visually identical everywhere EXCEPT the requested change". | aucune | — | fait | n/a |
| 30 | 2026-04-02 | préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : "Gros problème sur l'itération" = priorité absolue. Le fondateur attend que les itérations fonctionnent correctement — c'est un deal-breaker pour l'utilisation quotidienne du produit. | Audit Yann+Lucas lancé immédiatement + corrections v38 dans la même session | L'itération est le coeur de l'UX de Versimo — toute régression d'itération est traitée comme P0. | founder-prefs | docs/founder-preferences.md | fait | propagé |
| 30 | 2026-04-02 | préférence fondateur | - | [PRÉFÉRENCE FONDATEUR] : quand le fondateur dit "fais vérifier", il veut des audits agents formels avec rapport écrit — pas juste une vérification manuelle rapide. | Yann + Lucas audits lancés avec rapports dans docs/reviews/ | Les demandes de vérification = toujours des audits structurés avec rapport écrit, pas des checks informels. | founder-prefs | docs/founder-preferences.md | fait | propagé |
| 31 | 2026-03-31 | problème | P1 | font-light (300) illisible sur écrans standard DPI (non-Retina). 462 instances de font-light sur du texte text-sm+ à travers 35+ fichiers. Le fondateur n'avait pas le problème sur Retina mais des utilisateurs PC oui. | Remplacement font-light→font-normal sur text-sm+, conservation font-light sur text-xs | **RÈGLE @design/@fullstack : ne JAMAIS utiliser font-light (300) sur du texte text-sm ou plus grand. font-light réservé aux text-xs (captions, timestamps). Les écrans standard DPI rendent le 300 quasi invisible.** | appliqué |

## Session 2026-04-05 (Session 32 — Pipeline v49, pré-passe vision, compositing, page support)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| 32 | 2026-04-05 | problème | P0 | **Prompts v45 = code mort.** route.ts avait ses propres copies locales des builders qui masquaient generation-pipeline.ts. Les PREAMBLES de préservation n'ont JAMAIS tourné en prod. | Suppression ~600 lignes de code mort, import depuis generation-pipeline.ts | **RÈGLE : un seul fichier source de vérité pour les builders. JAMAIS de copie locale dans route.ts.** | règle-globale | CLAUDE.md, app/api/generate/route.ts | fait | propagé |
| 32 | 2026-04-05 | problème | P0 | **action:"edit" absent de route.ts.** gpt-image-1.5 était en mode "auto" et régénérait les scènes au lieu de les éditer. | action:"edit" présent dans generation-pipeline.ts tryOpenAIResponses | **RÈGLE @ia : tout changement de modèle → vérifier TOUS les paramètres API (action, fidelity, size).** | agent-spécifique | .claude/agents/ia.md | fait | propagé |
| 32 | 2026-04-05 | problème | P0 | **saveIterationBase fire-and-forget → Affiner cassé.** Replit autoscale tue le worker après réponse HTTP. L'image meublée n'était jamais sauvée. Affiner recevait une pièce vide. | 3 saveIterationBase passés en await avant response | **RÈGLE ABSOLUE @fullstack : sur Replit, TOUT save critique (images, DB) doit être await AVANT NextResponse.json(). Fire-and-forget = données perdues.** | règle-globale | CLAUDE.md | fait | propagé |
| 32 | 2026-04-05 | problème | P0 | **Tab-switch iOS tue les fetch.** AbortController.signal passé au fetch → le navigateur tue la connexion quand l'app passe en background. | Suppression du signal du fetch, BackgroundDisconnectError, toast "galerie" | **RÈGLE @fullstack : NE JAMAIS passer AbortController.signal au fetch pour les requêtes longues. Utiliser Promise.race pour le timeout.** | règle-globale | CLAUDE.md | fait | propagé |
| 32 | 2026-04-05 | problème | P1 | **Stale closure passe 2 .then().** results capturé comme [] dans le useCallback. Le .then() de passe 2 ne trouvait jamais le bon résultat pour mettre à jour versions. | setVersions appelé DANS setResults callback (fresh state) | **RÈGLE @fullstack : dans les .then() async de React, TOUJOURS utiliser le callback fonctionnel de setState pour accéder au state frais.** | règle-globale | CLAUDE.md | fait | propagé |
| 32 | 2026-04-05 | problème | P1 | **useEffect reset perPhotoStyles sur delete.** Le useEffect files→reset écrasait le reindex fait par le handler de suppression. | useEffect conditionnel (reset seulement si files.length > prev) | **RÈGLE @fullstack : les useEffect qui réagissent à un state doivent distinguer ADD vs DELETE.** | aucune | — | fait | n/a |
| 32 | 2026-04-05 | problème | P1 | **Ratio I/O cassé.** Seuil getOutputSize trop restrictif (1.45) : les photos 4:3 sortaient en carré. | Seuil abaissé à 1.2 — toute photo paysage sort en 1536x1024 | **RÈGLE @ia : vérifier le ratio I/O après chaque changement de getOutputSize.** | aucune | — | fait | n/a |
| 32 | 2026-04-05 | problème | P1 | **"wide-angle" dans DSLR_LINE encourageait le grand angle.** | Remplacé par "same focal length as input photo" | **RÈGLE : le DSLR_LINE ne doit JAMAIS prescrire une focale. Toujours "same as input".** | prompts | lib/generation-pipeline.ts | fait | propagé |
| 32 | 2026-04-05 | insistance | P0 | **Fondateur a demandé 3+ fois de fixer le tab-switch.** 4 tentatives avant de trouver la bonne approche (BackgroundDisconnectError au lieu de retry). | Approche finale : pas de retry (double facturation), toast galerie | Le retry d'un fetch qui coûte un crédit = INTERDIT. Toujours préférer un message informatif. | aucune | — | fait | n/a |
| 32 | 2026-04-05 | insistance | P0 | **Fondateur a demandé 3+ fois de fixer le PhotoAssociator pendant affinage/régénération.** | Condition masqué pendant pass2Pending/isRefining/isRegenerating + Pro-only | **RÈGLE : les composants contextuels doivent être masqués pendant TOUT état de travail, pas juste certains.** | aucune | — | fait | n/a |
| 32 | 2026-04-05 | préférence fondateur | P0 | **"Vélocité IA — ça prend 5 minutes, pas des semaines."** Le fondateur a recadré quand @ia estimait les efforts en semaines. | Plans recalibrés en minutes | Les agents doivent estimer en MINUTES, pas en jours/semaines. Un @fullstack code une feature en 15-30 min. | règle-globale | CLAUDE.md | fait | propagé |
| 32 | 2026-04-05 | préférence fondateur | - | **Les QA doivent tester du point de vue UTILISATEUR, pas du code.** Le fondateur a dit "QA ne regarde pas d'un point de vue utilisateur" quand le QA a dit PASS alors que l'overlay était au mauvais endroit. | QA reformulé en scénarios "ce que l'utilisateur VOIT" | **RÈGLE @qa : chaque scénario doit décrire ce que l'utilisateur VOIT sur son écran, pas juste les lignes de code.** | agent-spécifique | .claude/agents/qa.md | fait | propagé |
| 32 | 2026-04-05 | pattern | - | **Pipeline v49 : inventaire vision + compositing + best-of-2 conditionnel + SSIM local.** Gain cumulé +2-3 points sur les audits. | — | Architecture à capitaliser pour les futurs modèles IA. Le compositing est le filet de sécurité, pas le fix principal. | documentation | docs/ia/fix-gpt-image-1.5-plan.md | fait | n/a |
| 32 | 2026-04-05 | pattern | - | **resolveChooseOne côté serveur** force la variété entre générations. Le modèle ignorait les "choose one:" dans les prompts. | Pré-sélection aléatoire des alternatives avant envoi | Toujours pré-résoudre les alternatives côté serveur, ne pas laisser le modèle choisir. | prompts | lib/generation-pipeline.ts | fait | propagé |

## Session 2026-04-04 (Session 31 — Mode unifié + gpt-image-1.5)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Statut |
|---|---|---|---|---|---|---|---|
| 31 | 2026-04-04 | problème | P0 | **2 modes séparés (Standard + MerchantMode) causaient des bugs récurrents.** Chaque feature était implémentée dans un mode mais pas l'autre (split mode, itérations, session robuste). 6+ commits de bugs car on patchait le MAUVAIS mode (le fondateur est Pro → MerchantMode, les fixes étaient dans Standard). | Fusion complète en 1 seul flow dans page.tsx. MerchantMode retiré. | **RÈGLE ABSOLUE : JAMAIS 2 codebases pour le même parcours utilisateur. Si Pro et Starter font la même chose (upload→style→générer→résultat), c'est UN SEUL composant avec des conditions de visibilité par plan.** | appliqué |
| 31 | 2026-04-04 | problème | P0 | **getServerSession() retourne null sporadiquement sur Replit.** Cause de "Connectez-vous pour accéder aux itérations" malgré session Pro active. Patché 6 fois côté /api/generate sans effet car le fondateur passait par /api/dossier (MerchantMode). | lib/session.ts : getSessionRobust() avec fallback getToken(). Appliqué à /api/generate ET /api/dossier. | **RÈGLE @fullstack : NE JAMAIS patcher un seul endpoint quand le problème est systémique. Créer un helper centralisé et l'appliquer partout.** | appliqué |
| 31 | 2026-04-04 | problème | P0 | **gpt-image-1.5 détruit la géométrie avec les anciens prompts.** Préservation spatiale 2-3/10 (vs 7-8/10 avec gpt-image-1). Le modèle RÉGÉNÈRE au lieu d'ÉDITER car les prompts mettaient le STYLE en tête. | Prompts v45 : restructuration "Edit" en premier token, préservation AVANT style. PASS2_PREAMBLE ajouté aux 9 builders passe 2 (cause racine). | **RÈGLE @ia : les modèles IA créatifs (1.5, 2.0, etc.) nécessitent "Edit" + PRÉSERVATION en tête de prompt. Le style vient APRÈS. Les premiers tokens ont le plus d'influence.** | en cours de test |
| 31 | 2026-04-04 | problème | P0 | **handleRegenerate envoyait un blob: URL au lieu de base64.** Le serveur rejetait "invalid base64-encoded value". | Conversion blob→base64 via FileReader avant envoi. | **RÈGLE @fullstack : les blob: URLs sont locaux au navigateur. JAMAIS les envoyer au serveur. Toujours convertir en data URI base64 avant.** | appliqué |
| 31 | 2026-04-04 | problème | P1 | **Découverte avec 2 crédits gratuits classé Starter.** La logique vérifiait `credits > 0` → tout le monde avait maxPhotos=5 et 1 itération. | hasStarterAccess() vérifie l'existence d'au moins 1 achat complété en DB. | **RÈGLE @fullstack : distinguer les plans par HISTORIQUE D'ACHAT, pas par solde de crédits. Les crédits gratuits ne comptent pas.** | appliqué |
| 31 | 2026-04-04 | problème | P1 | **Crédits jamais mis à jour côté client.** maxPhotos ne diminuait pas après génération. L'utilisateur pouvait tenter de dépasser ses crédits. | Décrémentation optimiste + re-fetch après checkout Stripe + validation avant génération. | **RÈGLE @fullstack : tout state qui dépend du serveur (crédits, session, plan) doit être mis à jour APRÈS chaque action qui le modifie.** | appliqué |
| 31 | 2026-04-04 | problème | P1 | **Safety system rejection sur itérations.** Les prompts adjust contenaient "SURGICAL EDIT", "95%+ identical pixels", "LOCKED" — termes agressifs que le safety filter OpenAI rejette. | Adoucissement : formulations neutres ("Make a small, precise edit", "Keep everything else unchanged", "preserved"). | **RÈGLE @ia : JAMAIS de langage agressif dans les prompts (CAPS, termes militaires, "LOCKED", "DO NOT"). Utiliser des formulations descriptives neutres. Le safety filter est plus strict sur gpt-image-1.5.** | appliqué |
| 31 | 2026-04-04 | problème | P1 | **Split mode invisible pour 1 photo.** React 18 batching : setResults + setIsGenerating(false) dans le même microtask → 1 seul render → le loading n'affichait JAMAIS le résultat intermédiaire. | isGenerating reste true tant qu'il y a des pass2Pending. Les handlers pass2 vérifient et mettent à false quand tous terminés. | **RÈGLE @fullstack : avec React 18 automatic batching, si 2 state updates doivent produire 2 renders séparés, utiliser setTimeout(fn, 0) ou séparer les microtasks.** | appliqué |
| 31 | 2026-04-04 | insistance | P0 | **Le fondateur a signalé les mêmes 2 bugs 5+ fois (itération + split mode).** On patchait le mauvais fichier à chaque fois. | Fusion des modes (cause racine) + investigation du VRAI chemin d'exécution (MerchantMode, pas Standard). | **RÈGLE ABSOLUE : quand un bug est signalé 2+ fois, ARRÊTER de patcher et INVESTIGUER le vrai chemin d'exécution. Demander au fondateur : "Sur quel écran exactement ?" avant de toucher au code.** | appliqué |
| 31 | 2026-04-04 | préférence fondateur | P0 | **"On garde 1.5, on le fait marcher."** Le fondateur refuse d'abandonner gpt-image-1.5 par paresse. Si les résultats sont mauvais, c'est le prompt qui est mauvais, pas l'outil. | Rollback annulé, prompts v45 restructurés pour 1.5. | **RÈGLE ABSOLUE : NE JAMAIS abandonner un outil/modèle par paresse. Adapter les prompts au modèle. Si ça ne marche pas, investiguer POURQUOI et fixer.** | en cours de test |
| 31 | 2026-04-04 | préférence fondateur | - | **Pas de bouton × sur les résultats générés.** "À quoi ça sert ?" | Bouton × supprimé des résultats. | Les résultats générés sont le livrable final — pas de suppression impulsive. | appliqué |
| 31 | 2026-04-04 | préférence fondateur | - | **Dossiers PDF uniquement depuis /mes-biens.** Pas de bouton dossier sur la page de génération. | Étape 3 fusion supprimée des specs. | Le dossier est un export post-organisation, pas un output de génération. | appliqué |
| 31 | 2026-04-04 | préférence fondateur | - | **PhotoAssociator masqué après affinage/régénération.** Pas de sens de proposer "Associer à un bien" quand on travaille le résultat. | Condition ajoutée : masqué si versions > 1 ou regeneratedIndex. | L'association vient APRÈS le travail sur le visuel, pas pendant. | appliqué |
| 31 | 2026-04-04 | pattern | - | **Comparateur touch mobile cassé avec react-compare-slider v4.** Problème de touch events documenté dans les issues du package. | Remplacement par un comparateur custom avec Pointer Events API natifs. | **Préférer les implémentations custom pour les composants critiques UX plutôt que des librairies tierces qui peuvent avoir des bugs de compatibilité.** | appliqué |
| 31 | 2026-04-04 | pattern | - | **Les audits QA en "théorie" (logique du code) ne détectent pas les vrais bugs.** 6 QA ont dit PASS sur les itérations — le fondateur avait toujours le bug. La cause : on auditait le mauvais fichier. | Fusion des modes a résolu le problème structurel. | **Les audits QA doivent tracer le VRAI chemin d'exécution de l'utilisateur, pas juste la logique du code. Demander : "quel composant l'utilisateur VOIT-il ?"** | appliqué |

## Session 33 — 2026-04-05 (Versimo marathon prompts + multi-photo)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| 33 | 2026-04-05 | problème | P0 | **Multi-photo : 8 bugs cumulés** sur le pipeline génération (compteur crédits ne bouge pas, 1/3 image en génération, overlay mal positionné, mélange avant/après, race conditions abort + flicker, userCredits null skip check). Cause : pipeline conçu en mode séquentiel batch, MAX_CONCURRENT=2, état partagé entre runs. | MAX_CONCURRENT 2→5, CustomEvent detail.credits, tri par fileIndex, batchesCompleteRef, secondaryAbortRef, refund auto. F12 6 règles R1-R6 enregistrées. | **Tester les flows multi-éléments DÈS LA CONCEPTION, pas après les bugs.** Specs F12 doit être créée AVANT le code. | règle-globale | docs/product/functional-specs.md, app/page.tsx | fait | propagé |
| 33 | 2026-04-05 | problème | P0 | **Build cassé après refactoring** — 3 ESLint errors (currentProcessing, jobIdx, userCredits dep). Le fondateur a dû tester le build et signaler l'erreur. | Fix appliqué après vérification `npx next lint`. | **TOUJOURS lancer `npx next lint` avant de prétendre qu'un fix code est terminé.** Règle CLAUDE.md déjà documentée mais ignorée. | règle-globale | CLAUDE.md ligne ~408 | fait | propagé |
| 33 | 2026-04-05 | insistance | P0 | **Le fondateur a insisté 3 fois** pour qu'on cherche un fix prompt au lieu d'invoquer "limitation du modèle". 1er audit Yann/Lucas dit "limitation gpt-image-1.5" → fondateur recadre → refonte v53 (663→220 mots) → résultat amélioré. | Refonte complète passe 1 + passe 2 (v53/v54). | **JAMAIS dire "c'est la limitation du modèle" sans avoir audité et optimisé le prompt en profondeur.** Les agents doivent être briefés à chercher la cause RACINE prompt avant de blâmer le modèle. | agent-spécifique + founder-prefs | .claude/agents/ai-image-expert.md, .claude/agents/interior-architect.md, docs/founder-preferences.md | fait | propagé |
| 33 | 2026-04-05 | problème | P1 | **Audits QA en "théorie" insuffisants** — 6 QA ont dit PASS sur le pipeline mais le fondateur a eu un bug visuel concret (overlay mal positionné). Le QA testait le code, pas l'expérience visuelle. | Lancement systématique d'@ux après @qa pour valider le rendu visuel par persona. | **Pour tout fix UI : @qa (code) PUIS @ux (visuel) PUIS test persona.** Pas de raccourci. Pattern : quand le fondateur dit "tu es sûr ?" → c'est qu'il manque l'audit visuel. | agent-spécifique | .claude/agents/qa.md, .claude/agents/ux.md, CLAUDE.md | fait | propagé |
| 33 | 2026-04-05 | pattern | P2 | **Découpage anti-timeout en multi-agents parallèles** très efficace. Au lieu d'1 agent monolithique qui timeout sur 600KB de sortie, lancer 3 agents focalisés (max 60-100 lignes chacun, fichiers limités). 2 timeouts évités cette session. | 3 agents QA en parallèle, 3 agents validation v53 en parallèle. | **Pour tout audit ou refactor sur fichier > 500 lignes : découper en N agents parallèles avec scope précis et limite de lignes de sortie.** Stratégie anti-timeout permanente. | règle-globale | CLAUDE.md règle anti-timeout | fait | propagé |
| 33 | 2026-04-05 | recommandation | P1 | **Spec F12 produite APRÈS les bugs** au lieu d'avant. PM doit produire les specs des features critiques DÈS LA DEMANDE, pas après que le fondateur signale les bugs. | F12 enregistrée par PM en réaction. | **Workflow : nouvelle feature → @product-manager produit la spec → @fullstack implémente → @qa teste contre la spec.** Pas de code sans spec pour les features critiques (paiement, génération, données utilisateur). | règle-globale + agent-spécifique | CLAUDE.md, .claude/agents/orchestrator.md, .claude/agents/product-manager.md | fait | propagé |
| 33 | 2026-04-05 | performance-ia | P1 | **gpt-image-1.5 perd focus après ~200 mots** dans le prompt. Les directives critiques (CEILING, COLUMN) en position mot 250+ étaient ignorées. Cause : modèle image perd attention sur les tokens tardifs. | Refonte prompts : 663→220 mots passe 1, 478→200 mots passe 2. STRUCTURE LOCK en premier mot. | **Sweet spot prompt gpt-image-1.5 = 150-220 mots.** Au-delà, les instructions tardives sont ignorées. Documenter dans .claude/agents/ai-image-expert.md comme règle absolue. | agent-spécifique | .claude/agents/ai-image-expert.md | fait | propagé |
| 33 | 2026-04-05 | préférence fondateur | - | **[PRÉFÉRENCE FONDATEUR] Refus catégorique de se satisfaire de 7-8/10.** "On se satisfait de 7.5 ?" — exigence systématique du 9.5+/10. Quand un agent rend un score < 9, le fondateur recadre. | Itérations multiples sur les prompts (v49→v54). | Le fondateur considère 8/10 comme un échec. Tout agent qui rend < 9 doit immédiatement proposer 3 fixes pour atteindre 9.5. | founder-prefs | docs/founder-preferences.md | fait | propagé |
| 33 | 2026-04-05 | préférence fondateur | - | **[PRÉFÉRENCE FONDATEUR] Refund automatique sur TOUT échec de crédit.** Annulation, erreur partielle, erreur totale, validation échouée — TOUJOURS rembourser. "Thomas teste l'annulation en 5 minutes." | F12.5 + handleCancelGeneration refund. | Pattern non-négociable : tout flow qui consomme un crédit doit avoir un chemin de remboursement automatique sur erreur. | founder-prefs | docs/founder-preferences.md, app/page.tsx | fait | propagé |
| 33 | 2026-04-05 | préférence fondateur | - | **[PRÉFÉRENCE FONDATEUR] Décrément crédits SYNCHRONE au clic, pas après la réponse API.** Le compteur doit bouger AVANT le premier appel API, sinon l'utilisateur a l'impression que rien ne se passe. CustomEvent avec detail.credits, pas fetch API (trop lent sur mobile). | setUserCredits + dispatchEvent au clic. | Tout indicateur d'état utilisateur (compteur, badge, status) doit être mis à jour de manière synchrone optimiste, pas après la réponse serveur. | founder-prefs | docs/founder-preferences.md | fait | propagé |
| 33 | 2026-04-05 | préférence fondateur | - | **[PRÉFÉRENCE FONDATEUR] "Fais une vraie vérification" = lancer le linter/build avant de confirmer.** Pas de "le fix devrait marcher" — exécuter `npx next lint` ou `npx tsc --noEmit`. | 3 erreurs ESLint trouvées et fixées après vérification réelle. | Pour tout fix code : exécuter le linter AVANT de répondre "fixé". Documenter l'output du linter dans le commit. | founder-prefs | docs/founder-preferences.md | fait | propagé |
| 33 | 2026-04-05 | préférence fondateur | - | **[PRÉFÉRENCE FONDATEUR] L'expérience IDENTIQUE peu importe le nombre d'éléments.** "Que je upload 1 ou 5 photos, l'expérience est exactement la même." Pas de UX dégradée sur les cas multiples. | F12 R6 : universalité 1-5 photos, tous tiers. | Toute feature qui supporte 1 à N éléments doit avoir une UX strictement identique (pas de "en attente", pas de batches visibles). | founder-prefs | docs/founder-preferences.md | fait | propagé |

## Session 34 — 2026-04-07 — Fix BR-3 + BR-4 (régression session 32 non propagée à l'itération)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| 34 | 2026-04-07 | problème | P0 | **BR-3 — Affiner/régénérer une photo verrouille les autres.** Tous les flags `isRefining`/`isRegenerating`/`refineError`/`refineWarnings`/`lastRefineComment`/`refineElapsed`/`secondaryAbortRef` étaient déclarés comme single states globaux dans `app/page.tsx`. La désactivation des boutons (`disabled={isRegenerating}`) lisait ces globaux → toute opération sur la photo #1 désactivait les boutons des photos #2/#3. Viole la préférence fondateur "expérience IDENTIQUE peu importe le nombre d'éléments". | Refactor en `Set<number>` + `Map<number, ...>` indexés par `resultIndex`. Per-photo AbortControllers via `secondaryAbortByIndexRef: Map<number, AbortController>`. Timer unique recalcule les Maps elapsed à partir de `refineStartTimesRef.current`. handleRefineRetry/Modify prennent l'index en paramètre. UI consume `isThisRefining`/`isThisRegenerating` calculés par index. | **Pattern à propager** : tout state lié à une opération sur un élément d'une liste doit être indexé par l'identifiant de l'élément, JAMAIS un single state global. Quand on a 1+ flag du type `is{Action}` + `{action}Index` dans le même composant, c'est un anti-pattern → migrer vers `Set<index>`. À auditer dans tous les autres composants Versimo qui gèrent des collections (galerie, dossiers, mes-biens). | règle-globale | app/page.tsx (fait), CLAUDE.md (propagé 2026-04-07 — règle STATE INDEXE), .claude/agents/fullstack.md (framework file — permission denied, voir CLAUDE.md Versimo local) | fait | propagé |
| 34 | 2026-04-07 | problème | P0 | **BR-4 — Tab-switch iOS pendant itération → BACKGROUND_DISCONNECT.** La session 32 avait corrigé le tab-switch iOS UNIQUEMENT pour `handleGenerate` (génération principale) en catchant `BackgroundDisconnectError` et affichant un toast "votre visuel sera dans votre galerie". Le fix n'a JAMAIS été propagé à `handleRefine` ni `handleRegenerate` → l'erreur tombait dans le bloc générique "Votre itération n'a pas été consommée" (anxiogène, pas actionnable). | Ajout catch `BackgroundDisconnectError` dédié dans `handleRefine` et `handleRegenerate` → toast info "Affinage/Régénération en cours en arrière-plan. Retrouvez votre visuel dans votre galerie sous peu". Pas de double-facturation (no retry). Bonus : refund crédit explicite sur erreur générique de régénération (manquait). | **Pattern à propager** : quand on corrige un bug sur un handler de fetch, AUDITER tous les autres handlers de fetch du même fichier (et fichiers voisins) pour vérifier que la correction est nécessaire ailleurs. Les régressions silencieuses viennent toujours de fixes non propagés aux chemins secondaires. **À ajouter dans le checklist @qa** : "tout fix sur un handler X impose un grep sur les autres handlers du même domaine". | règle-globale | app/page.tsx (fait), CLAUDE.md (propagé 2026-04-07 — règle PROPAGATION CROSS-HANDLER), .claude/agents/qa.md (framework file — permission denied) | fait | propagé |
| 34 | 2026-04-07 | recommandation | P1 | **Audit cross-handler obligatoire après tout fix d'un handler de fetch/réseau.** Sessions 31/32/33 ont produit des fixes parfaits sur le handler principal (handleGenerate) mais ces fixes n'ont jamais été audités sur les handlers secondaires (handleRefine, handleRegenerate). Résultat : 2 BR P0 en session 34 sur du code "déjà corrigé". | À introduire dans le protocole @fullstack et @qa : un fix réseau sur un handler X déclenche obligatoirement un grep sur tous les `fetch(`/`resilientFetch(` du composant + un check des `catch (e: unknown)` voisins. | À documenter dans `.claude/agents/fullstack.md` section "Protocole post-fix réseau" : checklist 3 items (grep handlers voisins, vérifier catch BackgroundDisconnect, vérifier refund credit). | agent-spécifique | .claude/agents/fullstack.md, .claude/agents/qa.md (framework files — permission denied) ; couvert par CLAUDE.md règle PROPAGATION CROSS-HANDLER (propagé 2026-04-07) | fait | propagé |
| 34 | 2026-04-07 | problème | P0 | **BR-1 — Compteur "Génération en cours (1/3)" alors que les 3 photos tournent en parallèle.** Le label de loading utilisait `({results.length + 1}/{total})`, un compteur séquentiel issu de l'ancienne époque où les jobs étaient traités un par un. Depuis session 33, `MAX_CONCURRENT=5` permet à toutes les photos de tourner simultanément, mais le label n'a jamais été mis à jour. Viole F12 R6 (session 33) : "expérience identique 1 ou N photos". | Nouveau label dérivé de l'état réel de `results[]`. doneCount = `results.filter(r => !r.pass2Pending).length`. inProgress = total - doneCount. Affichage adaptatif : 1 photo → "Génération en cours…" sans fraction. 0 done → "X visuels en parallèle". Some done → "N/total terminés". Tout done → "Finalisation…". | **Pattern à propager** : tout label de progression doit dériver de l'état réel des données, JAMAIS d'un compteur séquentiel hérité d'une époque single-threaded. Quand on parallélise un traitement (MAX_CONCURRENT > 1), grep TOUS les labels qui contiennent `${index}/${total}` ou `progress` pour vérifier qu'ils ne supposent pas un ordre séquentiel. | règle-globale | app/page.tsx (fait), CLAUDE.md (propagé 2026-04-07 — règle LABELS POST-PARALLELISATION), .claude/agents/fullstack.md (framework — permission denied) | fait | propagé |
| 34 | 2026-04-07 | problème | P0 | **BR-2 — Photo en surface-only affiche l'AVANT au lieu du résultat passe 1 dans le loading tile.** Diagnostic CONTREDIT l'hypothèse initiale : le mapping API → result est correct (`generatedUrl=pass1Image`), et le step-results final affiche bien le slider avant/après. Le bug est dans le LOADING TILE : quand une photo surface-only termine pendant que d'autres photos meublées sont encore en passe 2, `isGenerating` reste `true` et le tile reste affiché. Le code utilisait `src={showPass1 ? partialResult.generatedUrl : filePreviewUrls[i]}`. Pour un résultat surface-only `done` (pass2Pending=false), `showPass1=false` → src tombait sur l'INPUT photo. L'utilisateur voyait sa photo originale + checkmark vert et croyait que le résultat était la photo "avant". | Nouvelle variable `tileSrc` qui priorise `doneResult.generatedUrl` quand `done=true`, avec fallback sur `partialResult.generatedUrl` (split-mode pass1) puis sur `filePreviewUrls[i]` seulement quand rien n'est encore fini. Le bug affecte aussi théoriquement les with-furniture qui terminent en premier dans un batch mixte, mais c'est moins visible car isGenerating bascule rapidement à false. | **Pattern à propager** : tout `<img src>` dans un loading state doit pouvoir afficher un résultat partiellement disponible. Le fallback "input image" doit être le DERNIER recours, pas le default. Quand un état mixte (some done, some pending) est possible, écrire le src en cascade : `done > partial > input`. | règle-globale | app/page.tsx (fait), CLAUDE.md (propagé 2026-04-07 — règle LOADING STATE CASCADE), .claude/agents/fullstack.md (framework — permission denied) | fait | propagé |
| 34 | 2026-04-07 | problème | P1 | **9 tests unit custom-prompt + 1 parse error object-storage — root cause unique : mocks vitest incorrects.** Les mocks utilisaient `vi.fn().mockImplementation(() => ({ ... }))` pour stubber des classes (`OpenAI`, `@replit/object-storage#Client`). Vitest 4 refuse de traiter un arrow function comme constructeur — `new OpenAI()` jette `TypeError: ... is not a constructor`. Les 9 assertions failing (U-CP-001 à U-CP-015) laissaient croire à des bugs métier (heuristiques isExclusive/allowWallMounted/warnings sanitaires manquantes) alors que TOUT le comportement est correct dans lib/custom-prompt.ts — ces heuristiques sont déléguées à GPT-4.1-mini. Le parse error sur object-storage.test.ts (`state.storedBlobs:` dans un object literal) était un copier-coller cassé du même template. | Remplacer les mocks par de vraies `class` : `class OpenAI { chat = { completions: { create: chatCreateMock } }; constructor() {} }`. Fix appliqué sur `tests/unit/generation/custom-prompt.test.ts` et `tests/unit/generation/object-storage.test.ts`. Baseline : 122/126 → 175/179 PASS après R1-R4. | **Pattern de triage** : un mock qui jette "is not a constructor" est TOUJOURS un problème de mock (pas de code métier). Ne pas partir chasser un bug métier avant d'avoir lu le warning vitest `"did not use 'function' or 'class' in its implementation"`. Ajouter à `.claude/agents/qa.md` : "Quand un mock utilise `new`, toujours utiliser `class` ou `function` traditionnel, jamais arrow function". | agent-spécifique | tests/unit/generation/custom-prompt.test.ts (fait), tests/unit/generation/object-storage.test.ts (fait), CLAUDE.md (propagé 2026-04-07 — règle MOCKS VITEST AVEC new), .claude/agents/qa.md (framework — permission denied) | fait | propagé |
| 34 | 2026-04-07 | pattern | - | **Refactor R1-R4 en approche additive sur un fichier partagé.** `app/page.tsx` (3141 lignes) était modifié en parallèle par @qa (ajout data-testid) pendant que @fullstack devait extraire scheduler + refund calculator. Au lieu de modifier le fichier (risque de conflit destructif + régression sur 3141 lignes), extraction en PURE functions dans `lib/` (scheduler, refund-calculator, generation-schema) avec tests exhaustifs. Le câblage dans page.tsx reste à faire dans une passe ultérieure — les modules sont des "specs exécutables" qui documentent le comportement attendu. | Créé : `lib/multi-photo-scheduler.ts` (11 tests), `lib/refund-calculator.ts` (12 tests), `lib/generation-schema.ts` + zod@4 (22 tests), export `isComplexRoom` dans `lib/generation-pipeline.ts` (8 tests). +53 tests unitaires, 0 modification de page.tsx. | **Pattern à capitaliser** : quand 2 agents doivent toucher le même fichier en parallèle, l'un des 2 DOIT faire son travail en additif (nouveau fichier) et différer le câblage. Ça préserve la parallélisation et évite les merge conflicts qui font perdre 30min. | pattern | — | fait | n/a |
| 34 | 2026-04-07 | préférence fondateur | P1 | **Objectif qualité : 100% tests PASS sans exception. "Le fondateur veut la perfection, 100% pass."** La consigne session 34 était claire : ne jamais laisser un test failing en "TODO plus tard". Même si le bug est dans le test et pas le code, corriger avant de passer à la suite. | Respecté : 175/179 PASS + 4 skipped intentionnels (tests documentaires TTL). | **À appliquer systématiquement** : tout `npx vitest run` qui se termine avec `failed > 0` doit bloquer le passage à la tâche suivante. Les skipped sont OK si intentionnels et commentés. | founder-prefs | docs/founder-preferences.md (propagé 2026-04-07 — section Session 34), CLAUDE.md (propagé 2026-04-07 — règle 100% TESTS PASS) | fait | propagé |
| 34 | 2026-04-07 | pattern | P1 | **Frilosité technique levée par la couverture tests.** @fullstack round 3 avait refusé de câbler R1+R2 dans `handleGenerate` en invoquant "risque régression > valeur" sur un fichier de 3141 lignes. Le fondateur a challenge cette décision. @fullstack round 5 a réussi le câblage en 2 commits atomiques avec ZÉRO régression, grâce aux 175 tests unitaires + 197 E2E mis en place session 34. Citation agent : "L'extraction R1+R2 était bien conçue dès l'origine ; le seul obstacle était la frilosité de la session précédente face à un handleGenerate de 470 lignes — frilosité levée par les 175 tests qui détectent immédiatement toute régression." | Câblage R1 (`runParallelPhotoJobs`) + R2 (`calculateRefund`) dans `app/page.tsx` handleGenerate. Streaming UX préservée via `onJobComplete` callback. 2 commits : `8318d76` (refund post-loop) + `f2fbf5e` (scheduler complet). | **Règle à propager** : dès lors qu'une couverture tests ≥ 80% est en place, un refactor structurel n'est plus "risqué" — il est mesurable et réversible. Le vrai risque de refactor = absence de tests, pas le code lui-même. Un agent qui refuse un refactor doit documenter un **obstacle technique précis** (ex : "R1 manque le callback X"), pas une excuse floue ("risque régression"). Les tests sont le filet de sécurité qui rend le refactor sûr. | règle-globale | CLAUDE.md (propagé 2026-04-07 — règle COVERAGE-DRIVEN REFACTOR section Regles de Developpement) | fait | propagé |
| 34 | 2026-04-07 | pattern | P2 | **Auth mocking E2E via `page.route` suffit pour la majorité des cas.** @qa round 4 a débloqué le test `credits-badge` et le test `E-G07 Stripe full flow` en mockant `/api/auth/session` et `/api/user/credits` via `page.route` (pas besoin de signer un vrai JWT NextAuth). Le webhook Stripe (server-to-server) est simulé via re-route de `/api/user/credits` après navigation sur `/?checkout=success` — c'est l'équivalent observable côté client. | Fixture `tests/e2e/fixtures/auth-fixture.ts` avec `installSessionMocks(page, opts)` helper réutilisable + fixture Playwright `authenticatedPage`. | **À documenter dans `.claude/agents/qa.md`** : pour tester un flow authentifié en E2E Playwright sans infrastructure de test complexe, la priorité est (1) mock `/api/auth/session` + APIs user, (2) cookie JWT signé uniquement si le code utilise `next-auth.session-token` directement au lieu de l'API. Pour un webhook server-to-server (Stripe, etc.), re-router l'endpoint qui consomme le résultat du webhook côté client = équivalent fonctionnel. | agent-spécifique | tests/e2e/fixtures/auth-fixture.ts (fait), .claude/agents/qa.md (framework — permission denied) | fait | propagé |


## Session 40 — 2026-04-09 (Pivot Versimo Marchand — Phase 0→2c complètes)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| 40 | 2026-04-09 | pattern | P0 | **Briefs courts = anti-timeout validé à grande échelle.** 12 agents lancés, 2 timeouts (creative-strategy 30min, ia 33min). Relance avec brief ultra-court : creative-strategy 2min (vs 30min), ia 8min (vs 33min). Pattern session 38 confirmé sur un volume 3x supérieur. | Tous les briefs limités à max 40 lignes, instructions anti-timeout en tête (max N min, 1 Write stub + Edit, pas de Glob exhaustif) | **Règle permanente** : tout brief agent DOIT commencer par un bloc ANTI-TIMEOUT avec : durée max, nombre max de Write/Edit, interdictions (Glob exhaustif, lecture de gros fichiers). Les agents textuels (creative-strategy, copywriter) sont les plus sensibles — max 8 min. | règle-globale | CLAUDE.md (règle anti-timeout étendue) | fait | propagé |
| 40 | 2026-04-09 | pattern | P1 | **Décomposition en batches parallèles = vélocité maximale.** Backend (2 agents parallèles : fondations + routes) + Frontend (1 agent séquentiel) + 3 pages complémentaires (1 agent) + copy (1 agent parallèle). 12 agents, ~30 commits, 0 régression, 0 conflit git. | Batch 1 : fullstack A (lib/) + fullstack B (routes/) en parallèle. Batch 2 : fullstack C (pages 1-3-6). Batch 3 : fullstack D (pages 4-5-7) + copywriter en parallèle. | **Règle** : pour un gros chantier d'implémentation, découper en batches par couche (DB/schemas → routes → pages) et paralléliser les couches indépendantes. Ne jamais donner plus de 5-6 fichiers à un seul agent. | pattern | docs/marchand-pivot/orchestration-plan.md | fait | n/a |
| 40 | 2026-04-09 | préférence fondateur | P0 | **[PRÉFÉRENCE FONDATEUR] "On paie avant. Pas après."** Le paiement du parcours marchand (99€/bien) intervient à l'Étape 1, AVANT tout le parcours. Pas après validation, pas avant génération. À l'entrée. Le fondateur veut un engagement dès le départ — pas de parcours gratuit qui mène au paiement. | Corrigé dans functional-specs.md et user-flows.md. Les étapes 2-7 ne sont accessibles qu'après paiement validé. | **Règle permanente** : sur tout nouveau parcours payant, le paiement intervient À L'ENTRÉE sauf instruction contraire explicite du fondateur. Ne jamais proposer de "freemium jusqu'à l'étape X" sans validation. | founder-prefs | docs/founder-preferences.md, docs/marchand-pivot/product/functional-specs.md, docs/marchand-pivot/ux/user-flows.md | fait | propagé |
| 40 | 2026-04-09 | préférence fondateur | - | **[PRÉFÉRENCE FONDATEUR] "Jamais de MVP. Vision complète d'un coup."** Le fondateur a confirmé Q7.3 : pas de MVP, vision complète. Scope V1 = 7 étapes complètes, pas de version dégradée. | Scope complet implémenté (7 pages, 6 routes, 5 modules IA) | Les agents ne doivent JAMAIS proposer de "scope réduit V1" ou "MVP d'abord". La V1 EST la vision complète. La seule raison de couper : pas de valeur pour le persona. | founder-prefs | docs/founder-preferences.md | fait | propagé |
| 40 | 2026-04-09 | préférence fondateur | - | **[PRÉFÉRENCE FONDATEUR] "L'équipe décide ce qui est le plus cohérent" (architecture code).** Pour Q1.2 et Q7.2, le fondateur délègue les décisions techniques aux agents. Il ne veut pas trancher entre monorepo/sous-dossier/repo séparé. | Code intégré dans le projet existant (app/projet/, lib/marchand/, app/api/pro/) — décision @ia + exploration codebase | Les décisions d'architecture code sont déléguées aux agents techniques. Le fondateur valide le produit, pas l'implémentation. | founder-prefs | docs/founder-preferences.md | fait | propagé |
| 40 | 2026-04-09 | recommandation | P1 | **Coût API réel 4x plus élevé que l'estimation initiale.** @ia recherche estimait ~$2.71/bien, l'architecture détaillée avec tarifs vérifiés donne ~$9.76/bien (gpt-image-1.5 = $0.20/image en high res, pas $0.05). Marge reste solide (73%) mais alerte sur abonnés Pro à pleine consommation (marge ~1€/bien). | Documenté dans technical-architecture.md section 7 | **Règle** : toujours vérifier les tarifs API sur la page pricing officielle AVANT de valider un business model. Les estimations de recherche initiale sont des ordres de grandeur, pas des chiffres définitifs. | documentation | docs/marchand-pivot/ia/technical-architecture.md | fait | n/a |
| 40 | 2026-04-09 | problème | P1 | **6 API routes manquantes identifiées après implémentation frontend.** Les pages étapes 4/5/7 appellent des routes qui n'ont pas été créées par @fullstack B (GET lots, PUT qualify, PATCH recommendations/[id], POST/PUT description, POST dossier/pdf). Cause : le brief de @fullstack B listait 6 routes prioritaires mais pas les routes secondaires. | Documenté dans le handoff @fullstack D. À implémenter en session 41. | **Règle** : quand on implémente des pages frontend, TOUJOURS vérifier que TOUTES les routes appelées existent. Lister les routes manquantes dans le handoff. | agent-spécifique | app/api/pro/projects/[id]/ (6 routes à créer) | fait | n/a |

## Session 38 — 2026-04-08 (v58 NO-GO → v59 → v59 NO-GO → v60 + 62 gates Q-V)

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| 38 | 2026-04-08 | problème | P0 | **v58 régression catastrophique #234 bathroom (3.4/10 CAP 5).** Le roomFurnitureOverride bathroom contenait "ADD vanity ONLY if no vanity already exists" + le builder contenait "Compact by default: ONE vanity 60cm". gpt-image-1.5 ignore les conditionnels "ONLY if" et obéit à "ONE vanity 60cm" → élargit la pièce pour caser la vanity sur un couloir de 60cm de large. Aucune gate v55-v58 ne détectait cette contradiction Override+Builder. | v59 P0-1 : réécriture bathroom en hiérarchie STEP 1/2/3 conditionnelle à la largeur + suppression "Compact by default". Commit `370e8b7`. Validé empiriquement #239 (Yann 8.1 / Lucas 7.1). | **Règle à propager** : Builder + Override doivent être audités ENSEMBLE pour cohérence. Nouvelle gate catégorie I (Override/Builder Coherence) dans v59 anti-regression. **Pattern STEP 1/2/3** : gpt-image-1.5 obéit aux hiérarchies "IF narrow THEN X ELSE Y" quand (a) séquence numérotée, (b) Step 1 court, (c) items hyper-spécifiques avec quantités. À propager aux autres room types étroits. | règle-globale + pattern-propagation | CLAUDE.md, tests/unit/prompt-regression-v59-gates.test.ts, lib/room-types.ts | fait | propagé |
| 38 | 2026-04-08 | problème | P0 | **BR-5 — iteration base storage collision cross-photos (manifestation #3 bug Affiner).** `saveIterationBase(sessionId, base64)` keyait à `iteration-base/${sessionId}.jpg` = UN SEUL slot par session. Quand N photos générées dans la même session, les N appels écrasaient la même clé → le dernier (souvent Maximaliste) gagnait → tous les refines récupéraient cette image. Sessions 34 (BR-3) et 37 (BR-4) avaient fixé le state CLIENT mais jamais audité la couche SERVER STORAGE. | v59 BR-5 fix : clé dérivée de pass1Key (unique par photo) via helper `iterationBaseKey()`. Format sibling du pass1 cache : `sessions/{sid}/pass1_{ts}_iter.jpg`. 4 call-sites route.ts migrés + gates supprimés. 12 tests + 14 edge cases QA. Commits `6bf31b0` + `cc4a3a1`. | **Pattern à propager** : tout state SERVER STORAGE qui partage une clé entre opérations parallèles est un bug latent. Scanner périodiquement via regex sur `${sessionId}` + `${userId}` dans les fonctions de save. **Règle** : un state indexé par élément de liste doit l'être CÔTÉ CLIENT ET CÔTÉ STORAGE SERVER. | règle-globale | CLAUDE.md (règle "STATE INDEXE PAR ELEMENT" étendue au storage), lib/db.ts, app/api/generate/route.ts | fait | propagé |
| 38 | 2026-04-08 | problème | P0 | **BR-6 — Crop "Mes biens" 3 bugs (gate invisible 1/2 photos, modal affiche AVANT au lieu d'APRÈS, pas de rectangle sélectionnable).** Le composant CropModal utilisait react-easy-crop (paradigme pan+zoom, pas de sélection rectangulaire). Le bouton était gated sur `input_image_key` manquant sur la moitié des photos. L'API crop opérait sur `input_image_key` alors que l'intention était le visuel généré. | Migration complète vers react-image-crop (sélection rectangulaire libre + presets aspect). Gate sur `output_image_key`. API crop opère sur output + backup `original_output_key`. Uncrop backward compat sur les photos legacy avec `original_input_key`. 25 tests regression v59. Commit `1300f4a`. | **Pattern à propager** : avant tout audit UX, valider que la librairie UI supporte le paradigme attendu (rectangle vs zoom/pan). Nouveau gate v60 : crop opère sur l'image "livrable" (output), pas sur l'input brut. | pattern | components/CropModal.tsx, lib/db.ts, tests/unit/regression/br6-crop-output-image.test.ts | fait | propagé |
| 38 | 2026-04-08 | problème | P0 | **Replit ESM resolver bug — build échoue sur packages ESM-strict malgré install correct.** `react-image-crop` v11 a `"type": "module"` dans son package.json. Replit Deployments ne trouvait pas le package malgré package.json/lockfile committés + réinstall manuels. 2 stratégies échouées : (1) `transpilePackages` dans next.config.mjs — échec au stade webpack resolve, (2) vendoring runtime uniquement + types depuis npm — échec au stade tsc (types introuvables). | **Stratégie 3 = vendoring complet** (runtime + types + CSS) dans `components/vendor/react-image-crop/`. Runtime, types et CSS viennent tous du vendor path (`@/components/vendor/react-image-crop/index.js`). Reproduction stricte locale : `rm -rf node_modules/react-image-crop && npx next build` → ✓ Compiled successfully. Commits `125e699` + `8731430` + `bbda6a3`. | **Nouveau pattern de fallback Replit** : si npm install échoue mystérieusement et que les fichiers existent dans `node_modules/` localement, copier les fichiers dist dans `components/vendor/<pkg>/` est plus rapide qu'investiguer Replit. **Règle** : TOUJOURS reproduire le bug Replit localement avant de pusher un fix (via `rm -rf node_modules/<pkg> + next build`) — "ça passe chez moi" n'est pas une validation. | règle-globale | CLAUDE.md (règle vendoring fallback), components/vendor/react-image-crop/README.md | fait | propagé |
| 38 | 2026-04-08 | pattern | P0 | **Verbes d'action > verbes d'état sur gpt-image-1.5.** CLEANUP_V53 v58 disait "Room stays COMPLETELY EMPTY" → le modèle lit "stays" comme "reste dans son état actuel" → si l'input a des personnes, elles restent. Fix v59 : "Room MUST be rendered empty of all people, tools, and debris" — verbe d'action explicite. Validé empiriquement. | v59 P0 fix dans CLEANUP_V53 + v60 P0-A durcissement (anti-humain en 1ère position REMOVE + assertion finale dupliquée). Gate J01 détecte toute régression vers "stays/remains/keeps". | **Règle prompt-engineering permanente** : sur gpt-image-1.5, TOUJOURS préférer un verbe d'action ("MUST be rendered", "REMOVE", "PLACE") à un verbe d'état ("stays", "remains", "keeps"). À documenter dans `.claude/agents/ai-image-expert.md` comme règle absolue. | agent-spécifique + règle-globale | CLAUDE.md, .claude/agents/ai-image-expert.md (framework — permission denied), tests/unit/prompt-regression-v59-gates.test.ts (catégorie J) | fait | propagé |
| 38 | 2026-04-08 | pattern | P0 | **Conditionnels "ONLY if no X" sont ignorés par gpt-image-1.5.** Les clauses "ADD vanity ONLY if no vanity already exists" passent au bruit — le modèle lit "ADD vanity" et l'ajoute, puis "ONLY if..." en aside qu'il ignore. Cause racine #234. | v59 P0-1 : remplacement par hiérarchie STEP 1/2/3 explicite avec séquence numérotée + Step 1 court + quantités explicites. Pattern validé empiriquement #239. Gate N01 détecte toute régression vers "ONLY if". | **Règle prompt-engineering permanente** : sur gpt-image-1.5, NE JAMAIS utiliser de conditionnels latéraux "ONLY if X" pour une décision d'ajout. Utiliser des hiérarchies STEP 1/2/3 numérotées. À documenter dans `.claude/agents/ai-image-expert.md`. | agent-spécifique + règle-globale | CLAUDE.md, tests/unit/prompt-regression-v59-gates.test.ts, tests/unit/prompt-regression-v60-gates.test.ts | fait | propagé |
| 38 | 2026-04-08 | pattern | P0 | **Stratégie anti-timeout validée sur 4 agents.** Session 38 a eu 2 timeouts majeurs (1er @ia 50 min stuck en boucle Write puis "Prompt is too long", Yann timeout 23 min sur audit #231-235). Stratégie correctrice : (1) briefs courts, (2) lecture limitée max 7 fichiers, (3) interdiction de Glob exhaustif sur anciens audits, (4) Write minimal stub ≤30 lignes puis Edit incrémental, (5) sauvegarde commit après chaque étape, (6) bail-out précoce sur InputValidationError. | Appliqué : 2ème @ia a fini en 14 min avec brief plus court. Yann audit #238-241 rendu en 3:20 (vs 23 min précédent). Lucas audit #238-241 en 6 min. @ia v60 en 14 min avec 8 commits atomiques validés. | **Règle permanente** : quand un agent bloque en boucle, c'est un signe de **context exhaustion**, pas de bêtise. La solution = relancer avec brief plus court et scope plus étroit. Sauvegarder le travail technique déjà produit avant de relancer. Documenté dans CLAUDE.md "Règle absolue anti-timeout" avec les 6 principes. | règle-globale | CLAUDE.md (règle anti-timeout étendue), .claude/agents/_base-agent-protocol.md (framework — permission denied) | fait | propagé |
| 38 | 2026-04-08 | performance-ia | P0 | **"Prompt is too long" = context exhaustion.** Le 1er @ia a bloqué sur "InputValidationError: content parameter missing" en boucle pendant 50 min, puis a officiellement terminé avec "Prompt is too long" après 91 tool uses. Cause : lecture cumulée de gros fichiers (generation-pipeline.ts ~900 lignes, room-types.ts, Lucas report, images) → context window saturé → le Write tool call ne pouvait plus fitter le content de 451 lignes dans la requête restante. | Le 2ème @ia avec contexte vierge a fini le travail. Les commits critiques du 1er @ia étaient déjà sauvegardés avant le blocage (370e8b7 v59 fixes prompts + bump). | **Règle triage** : si un agent entre en boucle Write avec erreur "content parameter missing" ou "InputValidationError" répétée > 2 fois, c'est context exhaustion — STOP, sauvegarder ce qui est déjà commité, relancer un nouvel agent avec brief plus court. Jamais insister sur le même agent stuck. | règle-globale | CLAUDE.md (règle anti-timeout — diagnostic context exhaustion) | fait | propagé |
| 38 | 2026-04-08 | pattern | P1 | **Hiérarchie de confiance empirique > théorique.** v59 P0-1 bathroom STEP 1/2/3 était une hypothèse pragmatique @ia confiance 75% (R2 = risque "gpt-image-1.5 peut ignorer les STEP comme il ignore les ONLY if"). L'audit Yann+Lucas sur #239 (1 seul cas empirique) a VALIDÉ l'hypothèse → confiance remontée à 85%. Une seule génération réelle a plus de valeur que des heures de spéculation théorique. | Note Yann 8.1 / Lucas 7.1 sur #239 = validation empirique du pattern STEP conditional. Propagation recommandée aux autres room types en v61. | **Règle méta** : quand une hypothèse prompt-engineering est critique mais non démontrée (R2 @ia), privilégier un test empirique minimal (1-3 cas limites) à une longue spéculation théorique. Le feedback visuel de gpt-image-1.5 est la seule source de vérité. | pattern + founder-prefs | docs/ia/v59-summary.md, .claude/agents/ai-image-expert.md (framework — permission denied) | fait | propagé |
| 38 | 2026-04-08 | préférence fondateur | - | **[PRÉFÉRENCE FONDATEUR] "Fais un vrai test avant de confirmer."** Le fondateur a recadré 2 fois session 38 sur des confirmations prématurées : (1) Replit build fix — j'avais dit "tout vert" après test local avec node_modules complet, mais Replit ne trouve pas le package → j'ai dû ensuite supprimer le package du node_modules local pour reproduire l'état Replit et tester. (2) Audit gen #238-241 — le fondateur a insisté pour des agents qui vérifient les fixes empiriquement. | Pour Replit : reproduction stricte locale via `rm -rf node_modules/<pkg> + next build`. Pour audit : lancement Yann+Lucas avec stratégie anti-timeout. | **Règle permanente** : "tout vert chez moi" n'est jamais une validation suffisante quand l'environnement cible est différent (Replit, Docker, CI). TOUJOURS reproduire l'état exact de l'environnement cible localement avant de déclarer un fix. | founder-prefs + règle-globale | docs/founder-preferences.md, CLAUDE.md (règle reproduction stricte avant confirmation) | fait | propagé |
| 38 | 2026-04-08 | préférence fondateur | - | **[PRÉFÉRENCE FONDATEUR] "Ne me faites pas retester des choses déjà corrigées."** Session 38 a vu 3 manifestations du même bug Affiner (sessions 34 BR-3, 37 BR-4, 38 BR-5). À chaque fois le fix précédent était incomplet. Le fondateur a exprimé sa frustration : "Ca fait 3 fois que je remonte ce problème, je veux qu'il soit corrigé pour toute circonstance." | BR-5 : fix exhaustif sur la couche storage server (la seule qui n'avait pas été auditée) + tests edge cases couvrant TOUS les scénarios (split/non-split, refine-of-refine, regenerate+refine, anonymous, surfaces-only, race conditions, pass1Key non-standard). | **Règle permanente** : quand un bug est "remonté pour la 3e fois", STOP, ne pas appliquer le fix obvious, faire un audit exhaustif multi-couches (client state + server compute + server storage + cache) pour trouver LA cause racine qui englobe toutes les manifestations. | founder-prefs | docs/founder-preferences.md | fait | propagé |
